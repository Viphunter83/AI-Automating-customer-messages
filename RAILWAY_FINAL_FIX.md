# 🚨 ФИНАЛЬНОЕ РЕШЕНИЕ: Переменные окружения в Railway

**Дата:** 2025-11-27  
**Критическая проблема:** Railway не видит переменные окружения

---

## ⚠️ ВАЖНО: Где добавлять переменные?

**Переменные должны быть добавлены в СЕРВИС ПРИЛОЖЕНИЯ, а не в PostgreSQL!**

---

## 🎯 ПОШАГОВОЕ РЕШЕНИЕ (5 минут)

### Шаг 1: Откройте правильный сервис

1. Зайдите на [railway.app](https://railway.app)
2. Откройте проект **AI-Automating-customer-messages**
3. **ВАЖНО:** Выберите сервис **приложения** (тот, который запускает FastAPI)
   - НЕ выбирайте PostgreSQL!
   - Сервис приложения обычно называется как проект или "web"

### Шаг 2: Откройте Variables

1. В сервисе приложения нажмите **Variables** (или **Settings** → **Variables**)
2. Проверьте, есть ли уже переменные

### Шаг 3: Получите DATABASE_URL из PostgreSQL

1. Откройте сервис **PostgreSQL** (отдельный сервис)
2. Variables → скопируйте `DATABASE_URL` или `POSTGRES_URL`
3. **ИЗМЕНИТЕ ФОРМАТ:**
   - Было: `postgresql://postgres:password@host:5432/railway`
   - Нужно: `postgresql+asyncpg://postgres:password@host:5432/railway`

### Шаг 4: Добавьте переменные в сервис приложения

Вернитесь в сервис **приложения** → Variables → **+ New Variable**

**1. DATABASE_URL**
```
Имя: DATABASE_URL
Значение: postgresql+asyncpg://postgres:PASSWORD@HOST:PORT/railway
```
*Вставьте значение из PostgreSQL, изменив формат*

**2. OPENAI_API_KEY**
```
Имя: OPENAI_API_KEY
Значение: sk-your-api-key-here (замените на ваш реальный ключ)
```

**3. SECRET_KEY**
```
Имя: SECRET_KEY
Значение: your-secret-key-minimum-32-characters-long (замените на случайную строку)
```

**ВАЖНО при добавлении:**
- ✅ Выберите окружения: **Production**, **Preview**, **Development**
- ✅ Имена переменных в **ЗАГЛАВНЫХ БУКВАХ**
- ✅ Сохраните каждую переменную

### Шаг 5: Перезапустите сервис

1. Railway автоматически перезапустит после добавления переменных
2. Если нет → Settings → **Restart Service**

### Шаг 6: Проверьте логи

1. Deploy Logs → проверьте, что ошибка исчезла
2. Если ошибка осталась → проверьте:
   - Правильный ли сервис (приложение, не PostgreSQL)
   - Правильный ли формат DATABASE_URL (`postgresql+asyncpg://`)
   - Выбраны ли все окружения

---

## 🔍 Как проверить, что переменные добавлены правильно?

### В Railway Dashboard:

1. Сервис приложения → Variables
2. Должны быть видны:
   - ✅ `DATABASE_URL`
   - ✅ `OPENAI_API_KEY`
   - ✅ `SECRET_KEY`

### В логах Railway:

После перезапуска в логах не должно быть ошибки `ValidationError`.

---

## ⚠️ Частые ошибки

1. ❌ **Переменные добавлены в PostgreSQL сервис**
   - ✅ Должны быть в сервисе приложения!

2. ❌ **Неправильный формат DATABASE_URL**
   - ✅ Должен быть `postgresql+asyncpg://` (не `postgresql://`)

3. ❌ **Переменные не применены к окружению**
   - ✅ Выберите Production, Preview, Development

4. ❌ **Опечатки в именах**
   - ✅ Используйте ЗАГЛАВНЫЕ БУКВЫ: `DATABASE_URL`, `OPENAI_API_KEY`, `SECRET_KEY`

---

## 🎯 Альтернативное решение: Использовать ссылку на PostgreSQL

Если не хотите копировать DATABASE_URL вручную:

1. В сервисе приложения → Variables
2. Добавьте:
   ```
   DATABASE_URL = {{PostgreSQL.DATABASE_URL}}
   ```
3. **ВАЖНО:** После этого откройте переменную и вручную измените:
   - `postgresql://` → `postgresql+asyncpg://`
4. Сохраните

---

## ✅ После исправления

После добавления переменных:

1. Railway автоматически перезапустит сервис
2. Проверьте Deploy Logs - ошибка должна исчезнуть
3. Проверьте health endpoint:
   ```bash
   curl https://your-project.railway.app/health
   ```

---

## 📞 Если проблема осталась

1. Проверьте, что переменные добавлены в **правильный сервис** (приложение, не PostgreSQL)
2. Проверьте формат `DATABASE_URL` (`postgresql+asyncpg://`)
3. Проверьте, что переменные применены ко всем окружениям
4. Перезапустите сервис вручную

---

**Главное:** Переменные должны быть в сервисе **ПРИЛОЖЕНИЯ**, а не в PostgreSQL! 🚀


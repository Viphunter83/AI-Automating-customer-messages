# 🚀 Деплой бэкенда на Vercel

**Дата:** 2025-11-26  
**Статус:** Конфигурация готова

---

## 📋 Подготовка к деплою

### Созданные файлы

1. **`vercel.json`** - Конфигурация проекта Vercel
2. **`api/index.py`** - Serverless функция для FastAPI
3. **`requirements.txt`** - Python зависимости для Vercel

---

## 🔧 Конфигурация

### vercel.json
- Использует Python runtime 3.11
- Все запросы маршрутизируются в `api/index.py`
- Максимальное время выполнения: 30 секунд
- Память: 1024 MB
- Исключены ненужные файлы (frontend, node_modules, etc.)

### api/index.py
- Добавляет `backend/` в Python path
- Импортирует FastAPI app из `backend/main.py`
- Vercel автоматически обрабатывает ASGI приложения

---

## 📝 Переменные окружения

Необходимо настроить следующие переменные в Vercel:

### Обязательные
- `DATABASE_URL` - URL подключения к PostgreSQL
- `OPENAI_API_KEY` - API ключ OpenAI/ProxyAPI
- `SECRET_KEY` - Секретный ключ (минимум 32 символа)

### Опциональные
- `OPENAI_API_BASE` - Базовый URL API (по умолчанию: ProxyAPI)
- `OPENAI_MODEL` - Модель (по умолчанию: gpt-4o-mini)
- `DEBUG` - Режим отладки (по умолчанию: False)
- `LOG_LEVEL` - Уровень логирования (по умолчанию: INFO)
- `ALLOWED_ORIGINS` - Разрешенные источники для CORS

---

## 🚀 Шаги деплоя

### Вариант 1: Через Vercel CLI

1. Установите Vercel CLI:
   ```bash
   npm i -g vercel
   ```

2. Войдите в аккаунт:
   ```bash
   vercel login
   ```

3. Деплой:
   ```bash
   vercel --prod
   ```

### Вариант 2: Через MCP (рекомендуется)

Используйте MCP инструменты Vercel для деплоя.

---

## ⚠️ Важные замечания

### База данных
- Vercel Serverless Functions не поддерживают постоянные соединения
- Рекомендуется использовать внешнюю БД (Supabase, Neon, Railway)
- Убедитесь, что `DATABASE_URL` указывает на внешнюю БД

### WebSocket
- WebSocket endpoints (`/ws`) могут не работать на Vercel
- Рассмотрите использование Vercel Realtime или внешнего сервиса

### Scheduler
- APScheduler не будет работать на serverless функциях
- Используйте Vercel Cron Jobs для периодических задач
- Или внешний сервис для планировщика

### Файловая система
- Serverless функции имеют read-only файловую систему
- Не сохраняйте файлы локально
- Используйте внешнее хранилище (S3, Supabase Storage)

---

## 🔍 Проверка после деплоя

1. Проверьте health endpoint: `https://your-project.vercel.app/health`
2. Проверьте API docs: `https://your-project.vercel.app/docs`
3. Проверьте создание сообщения: `POST /api/messages/`

---

## 📊 Ограничения Vercel

- **Максимальное время выполнения:** 30 секунд (Hobby), 60 секунд (Pro)
- **Максимальный размер функции:** 250 MB
- **Холодный старт:** Первый запрос может быть медленным
- **Одновременные выполнения:** Ограничены планом

---

## ✅ Готово к деплою

Конфигурация готова. Можно приступать к деплою через MCP или Vercel CLI.


# 🚨 ФИНАЛЬНОЕ РЕШЕНИЕ: Переменные окружения Railway

**Дата:** 2025-11-27  
**Проблема:** Railway не видит переменные окружения - приложение не запускается

---

## ❌ Текущая ошибка

```
ValidationError: 3 validation errors for Settings
database_url - Field required
openai_api_key - Field required
secret_key - Field required
```

**Причина:** Переменные окружения не добавлены в Railway или добавлены в неправильный сервис.

---

## ✅ РЕШЕНИЕ: Два способа

### Способ 1: Через Railway Dashboard (РЕКОМЕНДУЕТСЯ)

**Время:** ~5 минут

#### Шаг 1: Откройте Railway Dashboard
1. Зайдите на [railway.app](https://railway.app)
2. Откройте проект **AI-Automating-customer-messages**

#### Шаг 2: Найдите сервис ПРИЛОЖЕНИЯ
В проекте должно быть 2 сервиса:
- **PostgreSQL** - база данных (❌ НЕ ЭТОТ!)
- **AI-Automating-customer-messages** или **web** - приложение (✅ ЭТОТ!)

**Нажмите на сервис ПРИЛОЖЕНИЯ** (не PostgreSQL!)

#### Шаг 3: Откройте Variables
1. В сервисе приложения → вкладка **Variables**
2. Или **Settings** → **Variables**

#### Шаг 4: Получите DATABASE_URL
1. Откройте сервис **PostgreSQL** (отдельный сервис)
2. Variables → найдите `DATABASE_URL` или `POSTGRES_URL`
3. Скопируйте значение
4. **ИЗМЕНИТЕ ФОРМАТ:**
   ```
   Было:  postgresql://postgres:password@host:5432/railway
   Нужно: postgresql+asyncpg://postgres:password@host:5432/railway
   ```

#### Шаг 5: Добавьте переменные
Вернитесь в сервис **ПРИЛОЖЕНИЯ** → Variables → **+ New Variable**

**Переменная 1: DATABASE_URL**
```
Имя: DATABASE_URL
Значение: postgresql+asyncpg://postgres:PASSWORD@HOST:PORT/railway
Окружения: ✅ Production ✅ Preview ✅ Development
```

**Переменная 2: OPENAI_API_KEY**
```
Имя: OPENAI_API_KEY
Значение: [ваш реальный ключ от ProxyAPI]
Окружения: ✅ Production ✅ Preview ✅ Development
```

**Переменная 3: SECRET_KEY**
```
Имя: SECRET_KEY
Значение: [случайная строка минимум 32 символа]
Окружения: ✅ Production ✅ Preview ✅ Development
```

#### Шаг 6: Перезапустите
Railway автоматически перезапустит после добавления переменных.

---

### Способ 2: Через Railway CLI

**Время:** ~3 минуты (если CLI уже авторизован)

#### Шаг 1: Авторизация
```bash
railway login
```

#### Шаг 2: Связь с проектом
```bash
railway link
```

#### Шаг 3: Выбор сервиса приложения
```bash
railway service
# Выберите сервис ПРИЛОЖЕНИЯ (не PostgreSQL!)
```

#### Шаг 4: Получение DATABASE_URL
```bash
# Переключитесь на PostgreSQL сервис
railway service
# Выберите PostgreSQL

# Получите переменные
railway variables

# Скопируйте DATABASE_URL и измените формат:
# postgresql://... → postgresql+asyncpg://...
```

#### Шаг 5: Переключитесь на сервис приложения
```bash
railway service
# Выберите сервис ПРИЛОЖЕНИЯ
```

#### Шаг 6: Добавьте переменные
```bash
# DATABASE_URL (замените значения!)
railway variables set DATABASE_URL="postgresql+asyncpg://postgres:PASSWORD@HOST:PORT/railway"

# OPENAI_API_KEY (замените на ваш реальный ключ!)
railway variables set OPENAI_API_KEY="sk-your-api-key-here"

# SECRET_KEY (замените на случайную строку минимум 32 символа!)
railway variables set SECRET_KEY="your-secret-key-minimum-32-characters-long"
```

#### Шаг 7: Проверка
```bash
railway variables
```

Должны быть видны все 3 переменные.

#### Шаг 8: Перезапуск
```bash
railway restart
```

---

## 🔍 Проверка результата

### В Railway Dashboard:
1. Сервис ПРИЛОЖЕНИЯ → Variables
2. Должны быть видны:
   - ✅ `DATABASE_URL`
   - ✅ `OPENAI_API_KEY`
   - ✅ `SECRET_KEY`

### В логах:
1. Deploy Logs → ошибка `ValidationError` должна исчезнуть
2. Должны появиться логи успешного запуска

### Health endpoint:
```bash
curl https://your-project.railway.app/health
```

Должен вернуть `{"status": "healthy"}` или подобное.

---

## ⚠️ ЧАСТЫЕ ОШИБКИ

### ❌ Ошибка 1: Переменные в PostgreSQL сервисе
**Проблема:** Переменные добавлены в сервис PostgreSQL  
**Решение:** Удалите их из PostgreSQL и добавьте в сервис ПРИЛОЖЕНИЯ

### ❌ Ошибка 2: Неправильный формат DATABASE_URL
**Проблема:** Используется `postgresql://` вместо `postgresql+asyncpg://`  
**Решение:** Измените формат на `postgresql+asyncpg://`

### ❌ Ошибка 3: Переменные не применены к окружению
**Проблема:** Выбрано только Production  
**Решение:** Выберите все окружения: Production, Preview, Development

### ❌ Ошибка 4: Опечатки в именах
**Проблема:** `database_url`, `DATABASE_URL_`, `DATABASE-URL`  
**Решение:** Используйте точно `DATABASE_URL` (заглавными буквами)

---

## 📋 Чеклист

- [ ] Открыт Railway Dashboard или авторизован Railway CLI
- [ ] Найден сервис ПРИЛОЖЕНИЯ (не PostgreSQL)
- [ ] Получен DATABASE_URL из PostgreSQL
- [ ] Изменен формат DATABASE_URL на `postgresql+asyncpg://`
- [ ] Добавлена переменная `DATABASE_URL` в сервис приложения
- [ ] Добавлена переменная `OPENAI_API_KEY` в сервис приложения
- [ ] Добавлена переменная `SECRET_KEY` в сервис приложения
- [ ] Выбраны все окружения (Production, Preview, Development)
- [ ] Перезапущен сервис
- [ ] Проверены логи - ошибка исчезла

---

## ✅ После исправления

После добавления переменных:

1. Railway автоматически перезапустит сервис
2. Deploy Logs → ошибка должна исчезнуть
3. Приложение должно успешно запуститься
4. Health endpoint должен работать

---

**ГЛАВНОЕ:** 
- Переменные должны быть в сервисе **ПРИЛОЖЕНИЯ**, а не в PostgreSQL!
- DATABASE_URL должен иметь формат `postgresql+asyncpg://`
- Используйте реальные значения, а не примеры!

🚀 **Время на исправление:** ~5 минут


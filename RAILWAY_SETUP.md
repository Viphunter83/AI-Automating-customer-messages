# 🚂 Настройка Railway для базы данных

**Дата:** 2025-11-26

---

## 📋 Railway для PostgreSQL

Railway - отличный выбор для PostgreSQL базы данных:
- ✅ Простой деплой
- ✅ Автоматические бэкапы
- ✅ Бесплатный план ($5 кредитов в месяц)
- ✅ Простое управление через Dashboard

---

## 🚀 Шаги настройки Railway

### Шаг 1: Создание аккаунта и проекта

1. Зайдите на [railway.app](https://railway.app)
2. Войдите через GitHub/GitLab/Email
3. Создайте новый проект: **New Project**

### Шаг 2: Добавление PostgreSQL

1. В проекте нажмите **+ New**
2. Выберите **Database** → **Add PostgreSQL**
3. Railway автоматически создаст PostgreSQL инстанс

### Шаг 3: Получение DATABASE_URL

1. Откройте созданную БД
2. Перейдите на вкладку **Variables**
3. Скопируйте значение `DATABASE_URL` или `POSTGRES_URL`

**Формат URL:**
```
postgresql://postgres:PASSWORD@HOST:PORT/railway
```

**Для asyncpg (наш случай):**
```
postgresql+asyncpg://postgres:PASSWORD@HOST:PORT/railway
```

### Шаг 4: Применение миграций

После получения DATABASE_URL, примените миграции:

**Локально (для тестирования):**
```bash
export DATABASE_URL="postgresql+asyncpg://postgres:PASSWORD@HOST:PORT/railway"
cd backend
alembic upgrade head
```

**Или через Railway CLI:**
```bash
railway run alembic upgrade head
```

---

## 🔧 Настройка переменных окружения

После создания БД на Railway, обновите переменные в Vercel:

```bash
DATABASE_URL=postgresql+asyncpg://postgres:PASSWORD@HOST:PORT/railway
```

---

## 📊 Railway CLI (опционально)

Установка Railway CLI:
```bash
npm i -g @railway/cli
railway login
```

Полезные команды:
```bash
railway status          # Статус проекта
railway logs            # Логи БД
railway variables       # Переменные окружения
railway connect        # Подключение к БД
```

---

## ✅ Преимущества Railway

- ✅ Простое управление через Dashboard
- ✅ Автоматические бэкапы
- ✅ Мониторинг использования
- ✅ Легкое масштабирование
- ✅ Интеграция с GitHub

---

## 💰 Цены

- **Free tier:** $5 кредитов в месяц (достаточно для разработки)
- **Hobby:** $5/месяц (для небольших проектов)
- **Pro:** $20/месяц (для production)

---

**Готово!** После настройки Railway, используйте полученный `DATABASE_URL` в Vercel.


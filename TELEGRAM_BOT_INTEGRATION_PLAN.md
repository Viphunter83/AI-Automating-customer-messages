# 🤖 План интеграции Telegram-бота для тестирования системы

**Дата создания:** 1 декабря 2025  
**Версия:** 1.0.0  
**Статус:** План готов к реализации

---

## 📋 Содержание

1. [Анализ текущей архитектуры](#анализ-текущей-архитектуры)
2. [Концепция интеграции](#концепция-интеграции)
3. [Архитектурное решение](#архитектурное-решение)
4. [Детальный план реализации](#детальный-план-реализации)
5. [Структура файлов](#структура-файлов)
6. [Конфигурация](#конфигурация)
7. [Тестирование](#тестирование)
8. [Риски и митигация](#риски-и-митигация)

---

## 🔍 Анализ текущей архитектуры

### Текущий flow обработки сообщений:

```
1. CRM/Platform → POST /api/messages/
   ├─ Headers: X-Webhook-URL (куда отправлять ответ)
   ├─ Body: {client_id, content}
   └─ → MessageProcessingService
       ├─ → MessageResponseService
       └─ → MessageDeliveryService
           └─ → WebhookSender → X-Webhook-URL
```

### Ключевые компоненты:

1. **`/api/messages/`** - основной endpoint для приема сообщений
2. **`MessageProcessingService`** - обработка и классификация
3. **`MessageResponseService`** - создание ответов
4. **`MessageDeliveryService`** - доставка через webhooks
5. **`WebhookSender`** - отправка ответов обратно в платформу

### Преимущества текущей архитектуры:

✅ **Абстракция платформы** - система не знает о конкретной платформе  
✅ **Единая точка входа** - все сообщения проходят через один endpoint  
✅ **Гибкая доставка** - webhook URL передается динамически  
✅ **Модульность** - сервисы разделены по ответственности  

---

## 💡 Концепция интеграции

### Принципы интеграции:

1. **Не нарушать существующую архитектуру**
   - Telegram бот будет использовать тот же `/api/messages/` endpoint
   - Не дублировать логику обработки

2. **Адаптерный паттерн**
   - Создать адаптер для преобразования Telegram сообщений в формат системы
   - Создать адаптер для отправки ответов обратно в Telegram

3. **Минимальные изменения**
   - Добавить только новый модуль `telegram_bot/`
   - Не изменять существующие сервисы

4. **Единая точка конфигурации**
   - Все настройки Telegram в `config.py`
   - Переменные окружения для токена и настроек

---

## 🏗 Архитектурное решение

### Схема интеграции:

```
Telegram User → Telegram API → Telegram Bot Webhook
                                      ↓
                            Telegram Adapter (routes/telegram.py)
                                      ↓
                            POST /api/messages/
                                      ↓
                            [Существующая логика обработки]
                                      ↓
                            MessageDeliveryService
                                      ↓
                            Telegram Response Sender
                                      ↓
                            Telegram API → Telegram User
```

### Компоненты:

1. **Telegram Bot Handler** (`app/integrations/telegram/`)
   - Получение обновлений от Telegram
   - Преобразование в формат системы
   - Отправка ответов обратно

2. **Telegram Webhook Endpoint** (`app/routes/telegram.py`)
   - FastAPI endpoint для webhook от Telegram
   - Валидация подписи (опционально)

3. **Telegram Response Sender** (`app/integrations/telegram/sender.py`)
   - Отправка ответов в Telegram
   - Обработка ошибок Telegram API
   - Поддержка типизации сообщений (текст, фото, документы)

4. **Telegram Adapter** (`app/integrations/telegram/adapter.py`)
   - Преобразование Telegram Update → MessageCreate
   - Преобразование ответа системы → Telegram Message

---

## 📝 Детальный план реализации

### Этап 1: Подготовка инфраструктуры

**Время:** 30 минут

1. **Добавить зависимости**
   ```txt
   python-telegram-bot==20.7  # Async версия
   # или
   aiogram==3.1.1  # Более современная библиотека
   ```

2. **Создать структуру модуля**
   ```
   backend/app/integrations/
   ├── __init__.py
   ├── telegram/
   │   ├── __init__.py
   │   ├── bot.py          # Основной класс бота
   │   ├── adapter.py      # Адаптеры преобразования
   │   ├── sender.py       # Отправка ответов
   │   └── handlers.py     # Обработчики команд
   ```

3. **Добавить конфигурацию**
   - `TELEGRAM_BOT_TOKEN` в `.env`
   - `TELEGRAM_WEBHOOK_URL` (опционально)
   - `TELEGRAM_WEBHOOK_SECRET` (для безопасности)

### Этап 2: Создание адаптеров

**Время:** 1 час

1. **Telegram → System Adapter**
   ```python
   # Преобразование Telegram Update → MessageCreate
   def telegram_to_message(update: Update) -> MessageCreate:
       message = update.message
       return MessageCreate(
           client_id=f"telegram_{message.from_user.id}",
           content=message.text or message.caption or "",
           timestamp=datetime.fromtimestamp(message.date)
       )
   ```

2. **System → Telegram Adapter**
   ```python
   # Преобразование ответа системы → Telegram Message
   async def send_telegram_response(
       bot: Bot,
       chat_id: int,
       response_text: str,
       message_id: str
   ):
       await bot.send_message(
           chat_id=chat_id,
           text=response_text
       )
   ```

### Этап 3: Создание Telegram Bot Handler

**Время:** 2 часа

1. **Основной класс бота**
   - Инициализация с токеном
   - Регистрация обработчиков
   - Обработка входящих сообщений

2. **Интеграция с системой**
   - Вызов `/api/messages/` через HTTP клиент
   - Обработка ответа
   - Отправка через Telegram Response Sender

3. **Обработка ошибок**
   - Retry логика
   - Логирование
   - Fallback сообщения

### Этап 4: Создание Webhook Endpoint

**Время:** 1 час

1. **FastAPI endpoint**
   ```python
   @router.post("/webhook/telegram")
   async def telegram_webhook(update: Update):
       # Обработка обновления от Telegram
   ```

2. **Валидация подписи** (опционально)
   - Проверка секретного токена
   - Защита от подделки запросов

3. **Background обработка**
   - Использование BackgroundTasks
   - Не блокировать ответ Telegram

### Этап 5: Интеграция с MessageDeliveryService

**Время:** 1 час

1. **Создать Telegram Response Sender**
   - Наследование от базового класса (если есть)
   - Или отдельный сервис

2. **Модификация MessageDeliveryService**
   - Определение типа платформы (telegram/crm)
   - Выбор соответствующего sender'а

3. **Или использовать существующий WebhookSender**
   - Создать внутренний webhook endpoint для Telegram
   - Telegram Response Sender будет вызывать этот endpoint

### Этап 6: Добавление команд бота

**Время:** 30 минут

1. **Базовые команды**
   - `/start` - приветствие
   - `/help` - справка
   - `/status` - статус системы

2. **Административные команды** (опционально)
   - `/admin` - доступ к админ панели
   - `/stats` - статистика обработки

### Этап 7: Docker интеграция

**Время:** 30 минут

1. **Обновить docker-compose.yml**
   - Добавить переменные окружения для Telegram
   - Убедиться, что бот запускается вместе с backend

2. **Обновить .env.example**
   - Добавить примеры настроек Telegram

### Этап 8: Документация и тестирование

**Время:** 1 час

1. **Документация**
   - README для модуля telegram
   - Инструкции по настройке
   - Примеры использования

2. **Тестирование**
   - Unit тесты для адаптеров
   - Integration тесты для webhook
   - Ручное тестирование с реальным ботом

---

## 📁 Структура файлов

### Новая структура:

```
backend/app/
├── integrations/              # Новый модуль для интеграций
│   ├── __init__.py
│   └── telegram/
│       ├── __init__.py
│       ├── bot.py            # Основной класс Telegram бота
│       ├── adapter.py        # Адаптеры преобразования
│       ├── sender.py         # Отправка ответов в Telegram
│       └── handlers.py       # Обработчики команд
│
├── routes/
│   └── telegram.py           # Webhook endpoint для Telegram
│
└── config.py                 # Добавить настройки Telegram
```

### Изменения в существующих файлах:

```
backend/
├── main.py                   # Добавить router для telegram
├── requirements.txt          # Добавить python-telegram-bot
└── .env.example             # Добавить TELEGRAM_BOT_TOKEN
```

---

## ⚙️ Конфигурация

### Переменные окружения:

```env
# Telegram Bot Configuration
TELEGRAM_BOT_TOKEN=your_bot_token_here
TELEGRAM_WEBHOOK_URL=https://your-domain.com/webhook/telegram  # Опционально
TELEGRAM_WEBHOOK_SECRET=your_secret_here  # Опционально, для безопасности
TELEGRAM_ENABLED=True  # Включить/выключить бота
```

### Настройки в config.py:

```python
class Settings(BaseSettings):
    # ... существующие настройки ...
    
    # Telegram Bot
    telegram_bot_token: Optional[str] = None
    telegram_webhook_url: Optional[str] = None
    telegram_webhook_secret: Optional[str] = None
    telegram_enabled: bool = False
```

---

## 🧪 Тестирование

### Локальное тестирование:

1. **Polling режим** (для разработки)
   ```python
   # Использовать getUpdates вместо webhook
   # Проще для локальной разработки
   ```

2. **Webhook режим** (для production)
   ```python
   # Использовать ngrok для локального тестирования
   # ngrok http 8000
   # Установить webhook на ngrok URL
   ```

### Тестовые сценарии:

1. ✅ Отправка текстового сообщения
2. ✅ Получение автоматического ответа
3. ✅ Проверка классификации сообщения
4. ✅ Проверка эскалации (если нужно)
5. ✅ Проверка обработки ошибок
6. ✅ Проверка команд бота (/start, /help)

---

## ⚠️ Риски и митигация

### Риск 1: Конфликт с существующей логикой

**Проблема:** Изменение существующих сервисов может сломать CRM интеграцию

**Митигация:**
- Использовать только существующий `/api/messages/` endpoint
- Не изменять логику обработки
- Создать отдельный модуль `integrations/telegram/`

### Риск 2: Rate limiting Telegram API

**Проблема:** Telegram имеет ограничения на количество сообщений

**Митигация:**
- Использовать задержки между сообщениями
- Обрабатывать ошибки rate limiting
- Добавить retry логику

### Риск 3: Безопасность webhook

**Проблема:** Webhook может быть подделан

**Митигация:**
- Использовать секретный токен для валидации
- Проверять IP адреса Telegram (если возможно)
- Использовать HTTPS для webhook

### Риск 4: Производительность

**Проблема:** Обработка Telegram сообщений может замедлить систему

**Митигация:**
- Использовать BackgroundTasks для обработки
- Асинхронная обработка webhook
- Мониторинг производительности

---

## 🎯 Преимущества предложенного решения

### ✅ Органичность интеграции:

1. **Использует существующую архитектуру**
   - Не дублирует логику обработки
   - Использует те же сервисы

2. **Минимальные изменения**
   - Только новый модуль `integrations/telegram/`
   - Не затрагивает существующий код

3. **Легко расширяется**
   - Можно добавить другие платформы (WhatsApp, Viber)
   - Единый паттерн для всех интеграций

4. **Легко тестируется**
   - Изолированный модуль
   - Можно тестировать отдельно

5. **Легко отключается**
   - Флаг `TELEGRAM_ENABLED`
   - Не влияет на CRM интеграцию

---

## 📊 Оценка времени

**Общее время:** ~7-8 часов

- Этап 1: Подготовка инфраструктуры - 30 мин
- Этап 2: Создание адаптеров - 1 час
- Этап 3: Telegram Bot Handler - 2 часа
- Этап 4: Webhook Endpoint - 1 час
- Этап 5: Интеграция с DeliveryService - 1 час
- Этап 6: Команды бота - 30 мин
- Этап 7: Docker интеграция - 30 мин
- Этап 8: Документация и тестирование - 1 час

---

## 🚀 Следующие шаги

1. **Создать Telegram бота**
   - Написать @BotFather в Telegram
   - Получить токен бота

2. **Начать реализацию по плану**
   - Следовать этапам последовательно
   - Тестировать на каждом этапе

3. **Настроить webhook** (для production)
   - Использовать ngrok для локального тестирования
   - Настроить webhook на production сервере

4. **Добавить мониторинг**
   - Логирование всех операций
   - Метрики производительности

---

## 📚 Дополнительные ресурсы

- [python-telegram-bot документация](https://python-telegram-bot.org/)
- [Telegram Bot API](https://core.telegram.org/bots/api)
- [aiogram документация](https://docs.aiogram.dev/)

---

**Статус:** План готов к реализации  
**Приоритет:** Высокий  
**Зависимости:** Нет


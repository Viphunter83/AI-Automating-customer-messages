# 🚨 СРОЧНОЕ ИСПРАВЛЕНИЕ: Railway переменные окружения

**Дата:** 2025-11-27  
**Статус:** ⚠️ КРИТИЧНО - Приложение не запускается

---

## ❌ Проблема

Логи Railway показывают повторяющуюся ошибку:
```
ValidationError: 3 validation errors for Settings
database_url - Field required
openai_api_key - Field required
secret_key - Field required
```

**Это означает, что переменные окружения НЕ добавлены в Railway или добавлены в неправильный сервис!**

---

## ✅ РЕШЕНИЕ (5 минут)

### Шаг 1: Откройте Railway Dashboard

1. Зайдите на [railway.app](https://railway.app)
2. Войдите в аккаунт
3. Откройте проект **AI-Automating-customer-messages**

### Шаг 2: Найдите сервис ПРИЛОЖЕНИЯ

**ВАЖНО:** В проекте должно быть 2 сервиса:

1. **PostgreSQL** - база данных (❌ НЕ ЭТОТ!)
2. **AI-Automating-customer-messages** или **web** - приложение (✅ ЭТОТ!)

**Нажмите на сервис ПРИЛОЖЕНИЯ** (не PostgreSQL!)

### Шаг 3: Откройте Variables

1. В сервисе приложения найдите вкладку **Variables**
2. Или **Settings** → **Variables**
3. Проверьте список переменных

### Шаг 4: Получите DATABASE_URL

1. Откройте сервис **PostgreSQL** (отдельный сервис)
2. Variables → найдите `DATABASE_URL` или `POSTGRES_URL`
3. Скопируйте значение
4. **ИЗМЕНИТЕ ФОРМАТ:**
   ```
   Было:  postgresql://postgres:password@host:5432/railway
   Нужно: postgresql+asyncpg://postgres:password@host:5432/railway
   ```

### Шаг 5: Добавьте переменные в сервис ПРИЛОЖЕНИЯ

Вернитесь в сервис **ПРИЛОЖЕНИЯ** → Variables → **+ New Variable**

**1. DATABASE_URL**
- **Имя:** `DATABASE_URL`
- **Значение:** `postgresql+asyncpg://postgres:PASSWORD@HOST:PORT/railway` (из PostgreSQL, измененный формат)
- **Окружения:** ✅ Production ✅ Preview ✅ Development
- **Сохраните**

**2. OPENAI_API_KEY**
- **Имя:** `OPENAI_API_KEY`
- **Значение:** Ваш реальный ключ от ProxyAPI
- **Окружения:** ✅ Production ✅ Preview ✅ Development
- **Сохраните**

**3. SECRET_KEY**
- **Имя:** `SECRET_KEY`
- **Значение:** Случайная строка минимум 32 символа
- **Окружения:** ✅ Production ✅ Preview ✅ Development
- **Сохраните**

### Шаг 6: Перезапустите сервис

1. Railway автоматически перезапустит после добавления переменных
2. Если нет → Settings → **Restart Service**

### Шаг 7: Проверьте логи

1. Deploy Logs → ошибка должна исчезнуть
2. Если ошибка осталась → проверьте:
   - Правильный ли сервис (приложение, не PostgreSQL)
   - Правильный ли формат DATABASE_URL (`postgresql+asyncpg://`)
   - Выбраны ли все окружения

---

## 🔍 Как проверить правильность

### В Railway Dashboard:

1. Сервис **ПРИЛОЖЕНИЯ** (не PostgreSQL!)
2. Variables → должны быть видны:
   - ✅ `DATABASE_URL`
   - ✅ `OPENAI_API_KEY`
   - ✅ `SECRET_KEY`

### В логах Railway:

После перезапуска в Deploy Logs не должно быть ошибки `ValidationError`.

---

## ⚠️ ЧАСТЫЕ ОШИБКИ

### ❌ Ошибка 1: Переменные в PostgreSQL сервисе
**Проблема:** Переменные добавлены в сервис PostgreSQL  
**Решение:** Удалите их из PostgreSQL и добавьте в сервис ПРИЛОЖЕНИЯ

### ❌ Ошибка 2: Неправильный формат DATABASE_URL
**Проблема:** Используется `postgresql://` вместо `postgresql+asyncpg://`  
**Решение:** Измените формат на `postgresql+asyncpg://`

### ❌ Ошибка 3: Переменные не применены к окружению
**Проблема:** Выбрано только Production  
**Решение:** Выберите все окружения: Production, Preview, Development

### ❌ Ошибка 4: Опечатки в именах
**Проблема:** `database_url`, `DATABASE_URL_`, `DATABASE-URL`  
**Решение:** Используйте точно `DATABASE_URL` (заглавными буквами)

---

## 📸 Визуальная схема

```
Railway Dashboard
│
├── Проект: AI-Automating-customer-messages
│   │
│   ├── Сервис 1: PostgreSQL (БД)
│   │   └── Variables: DATABASE_URL (для копирования)
│   │
│   └── Сервис 2: AI-Automating-customer-messages (Приложение) ✅
│       └── Variables: ← ДОБАВИТЬ СЮДА!
│           ├── DATABASE_URL ✅
│           ├── OPENAI_API_KEY ✅
│           └── SECRET_KEY ✅
```

---

## 🎯 Чеклист

- [ ] Открыт Railway Dashboard
- [ ] Найден сервис ПРИЛОЖЕНИЯ (не PostgreSQL)
- [ ] Открыта вкладка Variables
- [ ] Получен DATABASE_URL из PostgreSQL
- [ ] Изменен формат DATABASE_URL на `postgresql+asyncpg://`
- [ ] Добавлена переменная `DATABASE_URL` в сервис приложения
- [ ] Добавлена переменная `OPENAI_API_KEY` в сервис приложения
- [ ] Добавлена переменная `SECRET_KEY` в сервис приложения
- [ ] Выбраны все окружения (Production, Preview, Development)
- [ ] Перезапущен сервис
- [ ] Проверены логи - ошибка исчезла

---

## ✅ После исправления

После добавления переменных:

1. Railway автоматически перезапустит сервис
2. Deploy Logs → ошибка должна исчезнуть
3. Health endpoint должен работать:
   ```bash
   curl https://your-project.railway.app/health
   ```

---

**ГЛАВНОЕ:** Переменные должны быть в сервисе **ПРИЛОЖЕНИЯ**, а не в PostgreSQL! 🚀

**Время на исправление:** ~5 минут


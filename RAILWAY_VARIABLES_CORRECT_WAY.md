# ✅ ПРАВИЛЬНЫЙ СПОСОБ: Переменные окружения в Railway

**Дата:** 2025-11-27  
**Проблема:** Не могу найти "Variables" в Railway Dashboard

---

## 🎯 ВАЖНО: Переменные находятся в СЕРВИСЕ, а не в ПРОЕКТЕ!

В Railway есть два уровня:
1. **Проект** (Project Settings) - общие настройки проекта
2. **Сервис** (Service Settings) - настройки конкретного сервиса (приложение, БД)

**Переменные окружения находятся в настройках СЕРВИСА, а не проекта!**

---

## 📍 Где найти переменные окружения

### Шаг 1: Вернитесь к списку сервисов

1. В Railway Dashboard найдите кнопку **← Back** или название проекта вверху
2. Вы должны увидеть список сервисов проекта:
   ```
   ┌─────────────────────────────────────┐
   │ AI-Automating-customer-messages     │ ← Проект
   │                                     │
   │ Сервисы:                            │
   │ ┌─────────────────────────────────┐ │
   │ │ PostgreSQL                      │ │ ← Сервис БД
   │ └─────────────────────────────────┘ │
   │ ┌─────────────────────────────────┐ │
   │ │ AI-Automating-customer-messages │ │ ← Сервис ПРИЛОЖЕНИЯ ✅
   │ └─────────────────────────────────┘ │
   └─────────────────────────────────────┘
   ```

### Шаг 2: Откройте сервис ПРИЛОЖЕНИЯ

**Нажмите на сервис ПРИЛОЖЕНИЯ** (не PostgreSQL!)

Это может быть:
- `AI-Automating-customer-messages`
- `web`
- `backend`
- Или другое название (но НЕ PostgreSQL)

### Шаг 3: Откройте Variables в сервисе

После открытия сервиса вы увидите вкладки:

```
┌─────────────────────────────────────┐
│ AI-Automating-customer-messages     │ ← Сервис
│                                     │
│ [Deploy] [Variables] [Settings]     │ ← Вкладки
│         ↑                           │
│      ЭТА ВКЛАДКА!                    │
└─────────────────────────────────────┘
```

**Нажмите на вкладку "Variables"** (не Settings!)

### Шаг 4: Добавьте переменные

В разделе Variables вы увидите:
- Список существующих переменных (если есть)
- Кнопку **+ New Variable** или **+ Add Variable**

---

## 🔍 Альтернативный путь через Settings

Если вкладки "Variables" нет, попробуйте:

1. Сервис → **Settings**
2. Найдите раздел **Variables** или **Environment Variables**
3. Там должна быть кнопка **+ New Variable** или **+ Add Variable**

---

## 📋 Пошаговая инструкция

### Вариант 1: Через вкладку Variables

1. **Railway Dashboard** → Проект `AI-Automating-customer-messages`
2. **Список сервисов** → Нажмите на сервис ПРИЛОЖЕНИЯ (не PostgreSQL!)
3. Вкладка **Variables** (вверху, рядом с Deploy и Settings)
4. **+ New Variable** или **+ Add Variable**
5. Добавьте переменные (см. ниже)

### Вариант 2: Через Settings

1. **Railway Dashboard** → Проект `AI-Automating-customer-messages`
2. **Список сервисов** → Нажмите на сервис ПРИЛОЖЕНИЯ
3. Вкладка **Settings**
4. Найдите раздел **Variables** или **Environment Variables**
5. **+ New Variable** или **+ Add Variable**
6. Добавьте переменные

---

## 📝 Переменные для добавления

### 1. DATABASE_URL

**Как получить:**
1. Откройте сервис **PostgreSQL** (отдельный сервис)
2. Variables → скопируйте `DATABASE_URL` или `POSTGRES_URL`
3. **ИЗМЕНИТЕ ФОРМАТ:**
   ```
   Было:  postgresql://postgres:password@host:5432/railway
   Нужно: postgresql+asyncpg://postgres:password@host:5432/railway
   ```

**Добавьте:**
- **Имя:** `DATABASE_URL`
- **Значение:** `postgresql+asyncpg://postgres:PASSWORD@HOST:PORT/railway`
- **Окружения:** ✅ Production ✅ Preview ✅ Development

### 2. OPENAI_API_KEY

- **Имя:** `OPENAI_API_KEY`
- **Значение:** Ваш реальный ключ от ProxyAPI
- **Окружения:** ✅ Production ✅ Preview ✅ Development

### 3. SECRET_KEY

- **Имя:** `SECRET_KEY`
- **Значение:** Случайная строка минимум 32 символа
- **Окружения:** ✅ Production ✅ Preview ✅ Development

---

## 🎯 Визуальная схема навигации

```
Railway Dashboard
│
├── Проект: AI-Automating-customer-messages
│   │
│   ├── Сервис 1: PostgreSQL
│   │   └── Variables → DATABASE_URL (для копирования)
│   │
│   └── Сервис 2: AI-Automating-customer-messages ✅
│       │
│       ├── [Deploy] ← Вкладка
│       ├── [Variables] ← ВКЛАДКА С ПЕРЕМЕННЫМИ! ✅
│       │   └── + New Variable
│       │       ├── DATABASE_URL ✅
│       │       ├── OPENAI_API_KEY ✅
│       │       └── SECRET_KEY ✅
│       │
│       └── [Settings] ← Альтернативный путь
│           └── Variables / Environment Variables
│               └── + New Variable
```

---

## ⚠️ ЧАСТЫЕ ОШИБКИ

### ❌ Ошибка 1: Находитесь в настройках проекта
**Проблема:** Вы в "Project Settings" → "General", "Usage", "Environments"  
**Решение:** Вернитесь к списку сервисов и откройте конкретный сервис

### ❌ Ошибка 2: Открыли сервис PostgreSQL
**Проблема:** Переменные добавлены в PostgreSQL сервис  
**Решение:** Откройте сервис ПРИЛОЖЕНИЯ (не PostgreSQL!)

### ❌ Ошибка 3: Ищете в разделе "Shared Variables"
**Проблема:** "Shared Variables" - это общие переменные проекта  
**Решение:** Нужны переменные конкретного сервиса, не общие!

---

## 🔍 Как проверить, что вы в правильном месте

### ✅ Правильно:
- Видите вкладки: `[Deploy] [Variables] [Settings]`
- Или в Settings видите раздел "Variables" или "Environment Variables"
- Это настройки конкретного сервиса, а не проекта

### ❌ Неправильно:
- Видите меню: "General", "Usage", "Environments", "Shared Variables"
- Это настройки проекта, а не сервиса!

---

## ✅ После добавления переменных

1. Railway автоматически перезапустит сервис
2. Deploy Logs → ошибка `ValidationError` должна исчезнуть
3. Приложение должно успешно запуститься

---

**ГЛАВНОЕ:** Переменные находятся в настройках **СЕРВИСА** (приложения), а не **ПРОЕКТА**! 🚀


# 🔧 Исправление проблемы Railway

**Дата:** 2025-11-26  
**Проблема:** Railway не может найти команду запуска

---

## 🐛 Проблема

Railway выдает ошибку:
```
✖ Команда запуска не найдена.
```

**Причина:** Railway ищет `main.py` или `app.py` в корне проекта, но наш бэкенд находится в папке `backend/`.

---

## ✅ Решение

Созданы два файла конфигурации:

### 1. `railway.toml` (основной)

```toml
[build]
builder = "NIXPACKS"

[deploy]
startCommand = "cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
```

### 2. `Procfile` (резервный)

```
web: cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT
```

---

## 🚀 Что делать дальше

### Вариант 1: Использовать railway.toml (рекомендуется)

1. Закоммитьте файлы в Git:
```bash
git add railway.toml Procfile
git commit -m "Add Railway configuration"
git push
```

2. Railway автоматически подхватит конфигурацию при следующем деплое

### Вариант 2: Настроить вручную в Railway Dashboard

1. Откройте ваш проект в Railway
2. Перейдите в **Settings** → **Service**
3. В разделе **Start Command** укажите:
```
cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT
```

### Вариант 3: Использовать только PostgreSQL на Railway

Если вы хотите использовать Railway только для БД, а бэкенд на Vercel:

1. Удалите деплой приложения из Railway
2. Оставьте только PostgreSQL
3. Используйте полученный `DATABASE_URL` в Vercel

---

## 📋 Важные переменные окружения

Убедитесь, что в Railway настроены переменные:

```
DATABASE_URL=postgresql+asyncpg://postgres:PASSWORD@HOST:PORT/railway
OPENAI_API_KEY=sk-your-key
SECRET_KEY=your-secret-key-minimum-32-characters
```

---

## ✅ Проверка

После деплоя проверьте:

```bash
# Health check
curl https://your-project.railway.app/health

# API docs
open https://your-project.railway.app/docs
```

---

## 💡 Рекомендация

**Для FastAPI лучше использовать Vercel:**
- ✅ Лучшая поддержка serverless
- ✅ Автоматическое масштабирование
- ✅ Edge Functions
- ✅ Бесплатный план с хорошими лимитами

**Railway лучше для:**
- ✅ PostgreSQL базы данных
- ✅ Долгоживущие процессы
- ✅ WebSocket соединения

**Идеальная комбинация:**
- Railway → PostgreSQL
- Vercel → FastAPI Backend

---

**Готово!** Закоммитьте файлы и перезапустите деплой на Railway.


# ✅ Чеклист перед коммитом в Git

## 🔒 Проверка безопасности

- [x] Удалены все реальные токены и API ключи из `docker-compose.yml`
- [x] Все секреты используют переменные окружения `${VAR:-default}`
- [x] `.env` файлы добавлены в `.gitignore`
- [x] Создан универсальный `.env.example` файл

## 🐳 Проверка Docker конфигурации

- [x] Команды для каждого сервиса настроены правильно:
  - `postgres`: стандартная команда PostgreSQL (без явной команды)
  - `redis`: `redis-server --appendonly yes`
  - `backend`: инициализация БД + `uvicorn main:app --reload`
  - `frontend`: `npm run dev` (для разработки)
- [x] Все переменные окружения используют значения по умолчанию
- [x] Health checks настроены для всех сервисов

## 📝 Документация

- [x] Удалена временная документация из папки `dokploy/`
- [x] Оставлены только важные файлы:
  - `README.md` - основная документация
  - `ARCHITECTURE.md` - архитектура проекта
  - `CONTEXT_FOR_CONTINUATION.md` - контекст для разработки
  - `.env.example` - пример переменных окружения

## ✅ Финальная проверка

Перед пушем убедитесь:
1. Все `.env` файлы в `.gitignore`
2. Нет реальных токенов/ключей в коде
3. Docker команды корректны
4. Документация актуальна


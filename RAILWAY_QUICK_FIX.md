# ⚡ Быстрое исправление Railway

**Проблема:** Railway не может найти переменные окружения

---

## 🚀 Быстрое решение (2 минуты)

### 1. Откройте Railway Dashboard
- [railway.app](https://railway.app)
- Проект: **AI-Automating-customer-messages**
- Сервис приложения (не PostgreSQL!)

### 2. Variables → + New Variable

Добавьте эти 3 переменные:

```
DATABASE_URL = {{PostgreSQL.DATABASE_URL}}
```
*Затем вручную измените `postgresql://` на `postgresql+asyncpg://`*

```
OPENAI_API_KEY = sk-tsnn3jx66aPvVtcGGte3MxRJ35DT8Uyz
```

```
SECRET_KEY = [сгенерируйте через команду ниже]
```

### 3. Сгенерируйте SECRET_KEY

```bash
python3 -c "import secrets; print(secrets.token_urlsafe(32))"
```

Скопируйте результат и вставьте в Railway как значение `SECRET_KEY`.

---

## ✅ Готово!

Railway автоматически перезапустит приложение. Проверьте логи!

---

**Подробная инструкция:** См. `RAILWAY_FIX_ENV.md`


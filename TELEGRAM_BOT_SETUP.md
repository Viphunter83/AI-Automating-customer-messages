# 🤖 Инструкция по настройке Telegram бота

## Быстрый старт

### 1. Создание бота

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям:
   - Введите имя бота (например: "AI Support Test Bot")
   - Введите username бота (должен заканчиваться на `bot`, например: `ai_support_test_bot`)
4. Сохраните полученный токен (формат: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

### 2. Настройка в проекте

Добавьте в файл `backend/.env`:

```env
TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather
TELEGRAM_ENABLED=True
```

### 3. Запуск

```bash
# Перезапустите Docker контейнеры
docker-compose down
docker-compose up -d --build
```

### 4. Использование

1. Найдите вашего бота в Telegram (по username, который вы указали)
2. Отправьте команду `/start`
3. Отправьте любое текстовое сообщение
4. Получите автоматический ответ от системы

## Проверка работы

После запуска проверьте логи:

```bash
docker-compose logs backend | grep -i telegram
```

Должны увидеть:
```
✅ Telegram bot started (polling mode)
```

## Режимы работы

### Polling режим (для локальной разработки)

Бот автоматически запускается в polling режиме, если:
- `TELEGRAM_ENABLED=True`
- `TELEGRAM_BOT_TOKEN` установлен
- `TELEGRAM_WEBHOOK_URL` не установлен

### Webhook режим (для production)

Для production используйте webhook:

```env
TELEGRAM_BOT_TOKEN=ваш_токен
TELEGRAM_ENABLED=True
TELEGRAM_WEBHOOK_URL=https://your-domain.com/api/integrations/telegram/webhook
TELEGRAM_WEBHOOK_SECRET=ваш_секретный_токен
```

## Команды бота

- `/start` - начать работу с ботом
- `/help` - показать справку

## Устранение проблем

### Бот не отвечает

1. Проверьте, что `TELEGRAM_ENABLED=True` в `.env`
2. Проверьте токен бота
3. Проверьте логи: `docker-compose logs backend`

### Ошибка подключения

- Убедитесь, что бот запущен (проверьте логи)
- Проверьте, что токен правильный
- Убедитесь, что интернет соединение работает

### Сообщения не обрабатываются

- Проверьте логи системы
- Убедитесь, что OpenAI API ключ настроен
- Проверьте подключение к базе данных

## Архитектура

```
Telegram User → Telegram API → Bot (Polling/Webhook)
                                      ↓
                            POST /api/messages/
                                      ↓
                            [Система обработки]
                                      ↓
                            Webhook → Telegram Response
                                      ↓
                            Telegram User
```

## Безопасность

- Токен бота хранится в `.env` (не коммитьте в git!)
- Webhook можно защитить секретным токеном
- Только приватные сообщения обрабатываются (не группы)


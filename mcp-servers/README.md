# MCP Серверы для Self-Hosted Supabase

Этот каталог содержит MCP (Model Context Protocol) серверы для работы с self-hosted Supabase.

---

## 📁 Структура

```
mcp-servers/
└── selfhosted-supabase-mcp/    # MCP сервер для self-hosted Supabase
    ├── dist/                    # Скомпилированный код
    ├── src/                     # Исходный код
    └── package.json             # Зависимости
```

---

## 🚀 Быстрый старт

### Для текущего проекта (Neiromatrius)

MCP сервер уже настроен в `.cursor/mcp.json`. Просто перезапустите Cursor.

### Для нового проекта

1. Скопируйте конфигурацию из `.cursor/mcp.json`
2. Обновите параметры подключения:
   - `--url` - URL вашего Supabase
   - `--anon-key` - Anon ключ
   - `--service-key` - Service Role ключ
   - `--db-url` - PostgreSQL connection string
   - `--jwt-secret` - JWT секрет

---

## 📋 Доступные инструменты

После подключения доступны инструменты для:
- Управления схемой БД
- Выполнения SQL запросов
- Управления миграциями
- Работы с Auth пользователями
- Работы со Storage
- Генерации TypeScript типов

---

## 🔧 Обновление MCP сервера

```bash
cd mcp-servers/selfhosted-supabase-mcp
git pull
npm install
npm run build
```

---

**Дата:** 8 декабря 2025








# 🔍 Отладка переменных окружения в Railway

**Дата:** 2025-11-27  
**Проблема:** Railway все еще не видит переменные окружения

---

## ❌ Текущая ошибка

```
ValidationError: 3 validation errors for Settings
database_url - Field required
openai_api_key - Field required
secret_key - Field required
```

---

## 🔍 Диагностика

### Проверка 1: Переменные добавлены в правильный сервис?

**Важно:** Переменные должны быть добавлены в **сервис приложения**, а не в PostgreSQL!

1. Откройте Railway Dashboard
2. Убедитесь, что вы в сервисе **приложения** (не PostgreSQL)
3. Variables → проверьте, что переменные видны

### Проверка 2: Правильные имена переменных?

Pydantic Settings читает переменные с учетом `case_sensitive=False`, поэтому работают оба варианта:
- `DATABASE_URL` или `database_url`
- `OPENAI_API_KEY` или `openai_api_key`
- `SECRET_KEY` или `secret_key`

**Рекомендуется использовать ЗАГЛАВНЫЕ БУКВЫ** для совместимости.

### Проверка 3: Переменные доступны в окружении?

Railway может не передавать переменные, если:
- Они добавлены в неправильный сервис
- Они не применены к окружению (production/preview)
- Они имеют неправильный формат

---

## ✅ Решение

### Вариант 1: Добавить переменные через Railway Dashboard (рекомендуется)

1. **Откройте Railway Dashboard**
2. **Выберите сервис приложения** (не PostgreSQL!)
3. **Variables** → **+ New Variable**
4. **Добавьте переменные:**

```
DATABASE_URL = postgresql+asyncpg://postgres:PASSWORD@HOST:PORT/railway
OPENAI_API_KEY = sk-tsnn3jx66aPvVtcGGte3MxRJ35DT8Uyz
SECRET_KEY = bCTR2fSwMxOvisSFpKh0qnQKt2NY4jJRLzR_5R8MI5Q
```

5. **Убедитесь, что выбраны окружения:** ✅ Production, ✅ Preview, ✅ Development

### Вариант 2: Использовать ссылку на PostgreSQL переменные

1. В сервисе приложения → Variables
2. Добавьте:

```
DATABASE_URL = {{PostgreSQL.DATABASE_URL}}
```

3. **ВАЖНО:** После этого вручную измените формат:
   - Откройте переменную `DATABASE_URL`
   - Измените `postgresql://` на `postgresql+asyncpg://`
   - Сохраните

### Вариант 3: Создать .env файл (не рекомендуется для production)

Если Railway не видит переменные, можно создать `.env` файл в корне проекта:

```bash
DATABASE_URL=postgresql+asyncpg://postgres:PASSWORD@HOST:PORT/railway
OPENAI_API_KEY=sk-tsnn3jx66aPvVtcGGte3MxRJ35DT8Uyz
SECRET_KEY=bCTR2fSwMxOvisSFpKh0qnQKt2NY4jJRLzR_5R8MI5Q
```

**⚠️ ВАЖНО:** Не коммитьте `.env` файл в Git! Он должен быть в `.gitignore`.

---

## 🔧 Проверка переменных в Railway

### Способ 1: Через Dashboard

1. Откройте сервис приложения
2. Variables → проверьте список переменных
3. Убедитесь, что они видны и имеют правильные значения

### Способ 2: Через Railway CLI

```bash
railway variables
```

Покажет все переменные для текущего проекта.

### Способ 3: Добавить debug логирование

Временно добавьте в `backend/main.py` перед запуском:

```python
import os
print("=== ENVIRONMENT VARIABLES ===")
print(f"DATABASE_URL: {'SET' if os.getenv('DATABASE_URL') else 'NOT SET'}")
print(f"OPENAI_API_KEY: {'SET' if os.getenv('OPENAI_API_KEY') else 'NOT SET'}")
print(f"SECRET_KEY: {'SET' if os.getenv('SECRET_KEY') else 'NOT SET'}")
```

Это поможет увидеть, видны ли переменные в Railway.

---

## 📋 Пошаговая инструкция

### Шаг 1: Откройте Railway Dashboard

1. [railway.app](https://railway.app)
2. Проект: **AI-Automating-customer-messages**
3. **ВАЖНО:** Выберите сервис **приложения** (не PostgreSQL!)

### Шаг 2: Проверьте текущие переменные

1. Variables → посмотрите список
2. Если переменных нет → добавьте их
3. Если переменные есть → проверьте значения

### Шаг 3: Добавьте/обновите переменные

**Обязательные:**
```
DATABASE_URL = [из PostgreSQL, формат: postgresql+asyncpg://...]
OPENAI_API_KEY = sk-tsnn3jx66aPvVtcGGte3MxRJ35DT8Uyz
SECRET_KEY = bCTR2fSwMxOvisSFpKh0qnQKt2NY4jJRLzR_5R8MI5Q
```

**Важно:**
- Имена в ЗАГЛАВНЫХ БУКВАХ
- Выберите окружения: Production, Preview, Development
- Сохраните изменения

### Шаг 4: Перезапустите сервис

1. Railway автоматически перезапустит после изменения переменных
2. Если нет → вручную: Settings → Restart Service

### Шаг 5: Проверьте логи

1. Deploy Logs → проверьте, что ошибка исчезла
2. Если ошибка осталась → проверьте формат переменных

---

## ⚠️ Частые ошибки

1. **Переменные добавлены в PostgreSQL сервис** → Должны быть в сервисе приложения!
2. **Неправильный формат DATABASE_URL** → Должен быть `postgresql+asyncpg://`
3. **Переменные не применены к окружению** → Выберите Production/Preview/Development
4. **Опечатки в именах** → Используйте ЗАГЛАВНЫЕ БУКВЫ

---

## 🎯 Быстрое решение

1. Откройте Railway Dashboard
2. Сервис приложения → Variables
3. Удалите все старые переменные (если есть)
4. Добавьте заново:
   - `DATABASE_URL` (из PostgreSQL, формат `postgresql+asyncpg://`)
   - `OPENAI_API_KEY`
   - `SECRET_KEY`
5. Выберите все окружения
6. Сохраните
7. Дождитесь перезапуска

---

**После этого ошибка должна исчезнуть!** 🚀


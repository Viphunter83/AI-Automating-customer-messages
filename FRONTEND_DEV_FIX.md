# 🔧 Исправление проблемы с Tailwind CSS в dev режиме

## Проблема

Next.js 14.2.33 имеет известную проблему с обработкой Tailwind CSS директив (`@tailwind`) в dev режиме при использовании Turbopack.

**Ошибка:**
```
Module parse failed: Unexpected character '@' (1:0)
> @tailwind base;
| @tailwind components;
| @tailwind utilities;
```

## ✅ Решение для локальной разработки

### Вариант 1: Использовать production build (рекомендуется)

Для локальной разработки можно использовать production build:

```bash
# В docker-compose.yml изменить команду для frontend:
command: npm run build && npm start
```

Или использовать отдельный скрипт:
```bash
docker-compose exec frontend npm run build && npm start
```

### Вариант 2: Обновить Next.js до последней версии

Проблема может быть исправлена в более новых версиях Next.js:

```bash
cd frontend
npm install next@latest
```

### Вариант 3: Использовать отдельный dev контейнер без Docker

Запускать frontend локально без Docker:

```bash
cd frontend
npm install
npm run dev
```

## ✅ Решение для production

**Production build работает корректно!** Проблема возникает только в dev режиме.

В `docker-compose.prod.yml` используется production build через Dockerfile, который работает без проблем.

## 📝 Текущий статус

- ✅ Production build работает корректно
- ⚠️ Dev режим имеет проблему с Tailwind CSS (известная проблема Next.js 14.2.33)
- ✅ Для production деплоя это не критично

## 🔄 Временное решение

Для локальной разработки можно:
1. Использовать production build: `npm run build && npm start`
2. Или запускать frontend локально без Docker
3. Или дождаться обновления Next.js с исправлением


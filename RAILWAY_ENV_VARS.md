# 🔧 Настройка переменных окружения в Railway

**Дата:** 2025-11-26  
**Проблема:** Railway не может найти обязательные переменные окружения

---

## ❌ Ошибка

```
pydantic_core._pydantic_core.ValidationError: 3 validation errors for Settings
database_url - Field required
openai_api_key - Field required
secret_key - Field required
```

---

## ✅ Решение: Настройка переменных в Railway

### Шаг 1: Откройте Railway Dashboard

1. Зайдите на [railway.app](https://railway.app)
2. Откройте ваш проект `AI-Automating-customer-messages`
3. Выберите сервис (не PostgreSQL, а сам сервис приложения)

### Шаг 2: Добавьте переменные окружения

1. Перейдите на вкладку **Variables**
2. Нажмите **+ New Variable**
3. Добавьте следующие переменные:

#### Обязательные переменные:

**1. DATABASE_URL**
```
Имя: DATABASE_URL
Значение: postgresql+asyncpg://postgres:PASSWORD@HOST:PORT/railway
```
*Получите значение из вашего PostgreSQL сервиса в Railway:*
- Откройте PostgreSQL сервис
- Variables → скопируйте `DATABASE_URL` или `POSTGRES_URL`
- Измените формат: `postgresql://` → `postgresql+asyncpg://`

**2. OPENAI_API_KEY**
```
Имя: OPENAI_API_KEY
Значение: sk-your-api-key-here (замените на ваш реальный ключ)
```
*Или ваш реальный ключ от ProxyAPI*

**3. SECRET_KEY**
```
Имя: SECRET_KEY
Значение: your-secret-key-minimum-32-characters-long-change-this-in-production
```
*Сгенерируйте случайную строку минимум 32 символа*

#### Опциональные переменные:

**4. OPENAI_API_BASE**
```
Имя: OPENAI_API_BASE
Значение: https://api.proxyapi.ru/openai/v1
```

**5. OPENAI_MODEL**
```
Имя: OPENAI_MODEL
Значение: gpt-4o-mini
```

**6. DEBUG**
```
Имя: DEBUG
Значение: False
```

**7. LOG_LEVEL**
```
Имя: LOG_LEVEL
Значение: INFO
```

---

## 🔗 Быстрый способ: Использовать переменные из PostgreSQL

Railway позволяет ссылаться на переменные других сервисов:

1. В разделе **Variables** вашего приложения
2. Нажмите **+ New Variable**
3. В поле **Value** используйте синтаксис:
   ```
   {{PostgreSQL.DATABASE_URL}}
   ```
   или
   ```
   {{PostgreSQL.POSTGRES_URL}}
   ```
4. Railway автоматически подставит значение из PostgreSQL сервиса

**Важно:** Не забудьте изменить формат на `postgresql+asyncpg://` если нужно!

---

## 📋 Полный список переменных

Скопируйте и вставьте в Railway Variables:

```
DATABASE_URL=postgresql+asyncpg://postgres:PASSWORD@HOST:PORT/railway
OPENAI_API_KEY=sk-your-api-key-here
SECRET_KEY=your-secret-key-minimum-32-characters-long-change-this
OPENAI_API_BASE=https://api.proxyapi.ru/openai/v1
OPENAI_MODEL=gpt-4o-mini
DEBUG=False
LOG_LEVEL=INFO
```

---

## ✅ После настройки

1. Railway автоматически перезапустит сервис
2. Проверьте логи - ошибка должна исчезнуть
3. Проверьте health endpoint:
   ```bash
   curl https://your-project.railway.app/health
   ```

---

## 🔍 Проверка переменных

После добавления переменных проверьте:

1. В Railway Dashboard → Variables
2. Убедитесь, что все переменные видны
3. Проверьте, что значения правильные (особенно DATABASE_URL)

---

## ⚠️ Важно

- ✅ Имена переменных должны быть **ЗАГЛАВНЫМИ БУКВАМИ**
- ✅ Используйте `DATABASE_URL`, а не `database_url`
- ✅ `SECRET_KEY` должен быть минимум 32 символа
- ✅ `DATABASE_URL` должен быть в формате `postgresql+asyncpg://`

---

**Готово!** После добавления переменных Railway перезапустит приложение автоматически! 🚀


# 🗄️ Руководство по подключению к Supabase

**Проект:** Neiromatrius  
**Дата:** 8 декабря 2025

---

## ⚠️ Важное уточнение

**Система использует прямое подключение к PostgreSQL через SQLAlchemy ORM, а не Supabase REST API.**

### Почему прямое подключение?

1. **Производительность:** Прямое подключение быстрее REST API
2. **Типобезопасность:** SQLAlchemy ORM обеспечивает проверку типов
3. **Удобство:** Использование ORM вместо ручных HTTP запросов
4. **Совместимость:** Supabase self-hosted поддерживает прямое PostgreSQL подключение

---

## 📋 Настройка подключения

### Шаг 1: Получение строки подключения из Supabase

1. Откройте **Supabase Dashboard**
2. Перейдите в **Settings** → **Database**
3. Найдите секцию **Connection String**
4. Выберите **URI** формат
5. Скопируйте строку подключения

**Пример строки из Supabase:**
```
postgresql://postgres:[YOUR-PASSWORD]@db.xxxxx.supabase.co:5432/postgres
```

### Шаг 2: Форматирование для приложения

**Добавьте `+asyncpg` после `postgresql`:**

```bash
# Было:
postgresql://postgres:password@db.xxxxx.supabase.co:5432/postgres

# Стало:
postgresql+asyncpg://postgres:password@db.xxxxx.supabase.co:5432/postgres
```

### Шаг 3: Установка переменной окружения

В Dokploy для backend сервиса добавьте:

```bash
DATABASE_URL=postgresql+asyncpg://postgres:YOUR_PASSWORD@db.xxxxx.supabase.co:5432/postgres
```

⚠️ **Важно:** 
- Замените `YOUR_PASSWORD` на реальный пароль
- Замените `db.xxxxx.supabase.co` на ваш хост Supabase
- Порт обычно `5432`

---

## 🧪 Тестирование подключения

### Локальное тестирование (перед деплоем)

```bash
# 1. Установите переменную окружения
export DATABASE_URL="postgresql+asyncpg://postgres:password@host:5432/postgres"

# 2. Запустите тест
python3 scripts/test_db_connection.py
```

**Ожидаемый результат:**
```
✅ Подключение к базе данных успешно!
✅ Все таблицы созданы (9 из 9)
✅ Все ENUM типы созданы (6 из 6)
```

### Тестирование после деплоя

```bash
# Health check
curl https://api.your-domain.com/api/health/db

# Должен вернуть:
# {"status":"healthy","database":"ok","pool":{...}}
```

---

## 🔧 Решение проблем

### Проблема: Database connection failed

**Возможные причины:**

1. **Неправильный формат DATABASE_URL**
   - ✅ Должен начинаться с `postgresql+asyncpg://`
   - ✅ Должен содержать правильный пароль
   - ✅ Должен содержать правильный хост и порт

2. **Неправильный пароль**
   - Проверьте пароль в Supabase Dashboard
   - Убедитесь, что нет лишних пробелов

3. **Хост недоступен**
   - Проверьте доступность хоста: `ping db.xxxxx.supabase.co`
   - Проверьте firewall правила
   - Убедитесь, что Supabase доступен из Dokploy сервера

4. **База данных не существует**
   - По умолчанию используется база `postgres`
   - Убедитесь, что база существует в Supabase

5. **Миграции не выполнены**
   - Выполните `database/migrations_supabase.sql` в Supabase SQL Editor
   - Проверьте создание таблиц

### Проблема: Invalid connection string

**Решение:**
```bash
# Проверьте формат:
# ✅ Правильно: postgresql+asyncpg://user:pass@host:port/db
# ❌ Неправильно: postgresql://user:pass@host:port/db (без +asyncpg)
# ❌ Неправильно: postgres://user:pass@host:port/db
```

---

## 📊 Структура подключения

```
Backend (FastAPI)
    ↓
SQLAlchemy ORM
    ↓
asyncpg (PostgreSQL driver)
    ↓
PostgreSQL (Supabase self-hosted)
```

**Не используется:**
- ❌ Supabase REST API
- ❌ Supabase JavaScript Client
- ❌ HTTP запросы к Supabase

**Используется:**
- ✅ Прямое PostgreSQL подключение
- ✅ SQLAlchemy ORM для работы с данными
- ✅ asyncpg для асинхронных запросов

---

## 🔒 Безопасность

1. **Храните DATABASE_URL в Secrets Dokploy**
   - Не храните в обычных переменных окружения
   - Используйте Secrets для чувствительных данных

2. **Ограничьте доступ к Supabase**
   - Настройте firewall правила
   - Используйте VPN если возможно
   - Ограничьте доступ по IP адресу Dokploy сервера

3. **Используйте сильные пароли**
   - Минимум 16 символов
   - Комбинация букв, цифр и символов

---

## 📚 Дополнительная информация

- [Supabase Connection Strings](https://supabase.com/docs/guides/database/connecting-to-postgres)
- [SQLAlchemy Documentation](https://docs.sqlalchemy.org/)
- [asyncpg Documentation](https://magicstack.github.io/asyncpg/)

---

**Дата создания:** 8 декабря 2025








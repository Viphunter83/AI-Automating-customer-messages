# ✅ Обновление конфигурации: порт 5437 обязателен

**Дата:** 8 декабря 2025

**Изменение:** Порт PostgreSQL должен быть **5437**, а не 5432!

---

## 🔧 Обновленные файлы

### 1. `backend/app/config.py`
- ✅ Порт по умолчанию изменен с `5432` на `5437`
- Комментарий: `# Порт БД (обязательно 5437!)`

### 2. `.cursor/mcp.json`
- ✅ Добавлен `--db-url` с портом **5437**
- Формат: `postgresql://postgres@supabase.dev.neiromatrius.zerocoder.pro:5437/postgres`
- **Примечание:** Если прямое подключение не работает (ошибка "Tenant or user not found"), MCP будет использовать REST API через функцию `execute_sql`

### 3. `dokploy/DOKPLOY_ENV_VARIABLES.md`
- ✅ Документация обновлена: `SUPABASE_PORT=5437` (ОБЯЗАТЕЛЬНО!)

---

## 📋 Переменные окружения для Dokploy

**Обязательно установите:**

```bash
# Порт БД (ОБЯЗАТЕЛЬНО 5437!)
SUPABASE_PORT=5437
```

**Для Backend сервиса:**

```bash
SUPABASE_USER=postgres
SUPABASE_PASSWORD=  # Может быть пустым
SUPABASE_HOST=  # Может быть пустым (используется "db" по умолчанию)
SUPABASE_PORT=5437  # ⚠️ ОБЯЗАТЕЛЬНО 5437!
SUPABASE_DB=postgres
```

---

## 🔍 Проверка после обновления

### 1. Перезапустите Cursor

После обновления `.cursor/mcp.json`:
1. Полностью закройте Cursor
2. Запустите Cursor снова
3. Дождитесь инициализации MCP сервера

### 2. Проверьте работу MCP

Попробуйте выполнить:

```bash
# В Cursor через MCP инструменты
list_tables
execute_sql: SELECT 1 as test;
```

### 3. Проверьте подключение Backend

Если Backend уже запущен, проверьте логи подключения к БД:
- Должен использоваться порт **5437**
- Если используется порт 5432 - проверьте переменные окружения в Dokploy

---

## ⚠️ Важные замечания

1. **Порт 5437 обязателен** - не используйте 5432!
2. **Если MCP не работает с прямым подключением** - это нормально, будет использоваться REST API через функцию `execute_sql`
3. **Для миграций** - используйте Supabase SQL Editor (не зависит от MCP)

---

**Дата:** 8 декабря 2025



# 🔄 Перезагрузка схемы PostgREST для функции execute_sql

**Проблема:** MCP сервер не видит функцию `execute_sql`, хотя она создана в БД.

**Причина:** PostgREST (Supabase REST API Gateway) не знает о новой функции, так как его схема не была перезагружена.

---

## ✅ Решение 1: Перезагрузка через Supabase Dashboard (рекомендуется)

1. **Откройте Supabase Dashboard**
2. **Перейдите в Database → Functions**
3. **Найдите функцию `execute_sql`** (если видна)
4. **Или перезагрузите PostgREST:**
   - В настройках Supabase найдите опцию "Reload PostgREST schema"
   - Или перезапустите сервис `postgrest` в Docker

---

## ✅ Решение 2: Перезагрузка через SQL (если есть прямой доступ)

Если у вас есть прямой доступ к БД (например, через `psql` или Supabase SQL Editor с правами суперпользователя):

```sql
-- Уведомить PostgREST о перезагрузке схемы
NOTIFY pgrst, 'reload schema';
```

**Важно:** Это работает только если у вас есть прямое подключение к PostgreSQL (не через REST API).

---

## ✅ Решение 3: Перезапуск PostgREST контейнера

Если Supabase развернут через Docker:

```bash
# Перезапустить PostgREST контейнер
docker restart <postgrest-container-name>

# Или через docker-compose
docker-compose restart postgrest
```

---

## ✅ Решение 4: Проверка функции через REST API напрямую

Проверьте, видит ли PostgREST функцию:

```bash
# Замените на ваши значения
SUPABASE_URL="http://supabase.dev.neiromatrius.zerocoder.pro:8000"
ANON_KEY="eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."

# Попробовать вызвать функцию через REST API
curl -X POST "${SUPABASE_URL}/rest/v1/rpc/execute_sql" \
  -H "apikey: ${ANON_KEY}" \
  -H "Authorization: Bearer ${ANON_KEY}" \
  -H "Content-Type: application/json" \
  -d '{"query": "SELECT 1", "read_only": true}'
```

**Ожидаемый результат:** `[{"?column?": 1}]` или похожий JSON массив

**Если ошибка:** PostgREST не видит функцию, нужно перезагрузить схему.

---

## 🔍 Диагностика

### Проверка 1: Функция существует в БД

```sql
-- В Supabase SQL Editor
SELECT 
    proname, 
    proargtypes,
    prosrc
FROM pg_proc 
WHERE proname = 'execute_sql';
```

**Ожидаемый результат:** Одна строка с функцией

### Проверка 2: Права на функцию

```sql
-- В Supabase SQL Editor
SELECT 
    grantee, 
    privilege_type 
FROM information_schema.routine_privileges 
WHERE routine_name = 'execute_sql';
```

**Ожидаемый результат:** Права для `authenticated`, `anon`, `service_role`

### Проверка 3: PostgREST видит функцию

```bash
# Через REST API (замените на ваши значения)
curl -X GET "${SUPABASE_URL}/rest/v1/rpc" \
  -H "apikey: ${ANON_KEY}" \
  -H "Authorization: Bearer ${ANON_KEY}"
```

**Ожидаемый результат:** Список доступных RPC функций, включая `execute_sql`

---

## 📋 Чеклист

- [ ] Функция `execute_sql` создана в БД
- [ ] Права на функцию предоставлены (`authenticated`, `anon`, `service_role`)
- [ ] PostgREST перезагружен (через Dashboard, SQL или перезапуск контейнера)
- [ ] Функция видна через REST API (`/rest/v1/rpc`)
- [ ] MCP сервер перезапущен (Cursor перезапущен)
- [ ] MCP команда `list_tables` работает

---

## ⚠️ Важно

**PostgREST кэширует схему БД** и не знает о новых функциях до перезагрузки схемы.

После создания функции в БД **обязательно** перезагрузите схему PostgREST одним из способов выше.

---

**Дата:** 8 декабря 2025






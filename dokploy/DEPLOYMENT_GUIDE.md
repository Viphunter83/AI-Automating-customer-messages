# 🚀 Руководство по деплою на Dokploy

**Проект:** Neiromatrius  
**Дата:** 8 декабря 2025

---

## 📋 Предварительные требования

1. ✅ Аккаунт в Dokploy
2. ✅ Supabase self-hosted база данных настроена
3. ✅ GitHub репозиторий с кодом проекта
4. ✅ Доступ к переменным окружения (см. `DOKPLOY_ENV_VARIABLES.md`)

---

## 🔧 Шаг 1: Подготовка базы данных

### 1.1 Выполнение миграций в Supabase

1. Откройте Supabase Dashboard → **SQL Editor**
2. Скопируйте содержимое файла `database/migrations_supabase.sql`
3. Вставьте в SQL Editor
4. Нажмите **Run** для выполнения миграций
5. Проверьте создание таблиц:
   ```sql
   SELECT table_name FROM information_schema.tables 
   WHERE table_schema = 'public' 
   AND table_name IN (
       'messages', 'classifications', 'response_templates', 'keywords',
       'operator_feedback', 'operator_session_logs', 'reminders',
       'chat_sessions', 'operator_message_reads'
   );
   ```

### 1.2 Получение строки подключения

В Supabase Dashboard → **Settings** → **Database**:
- Скопируйте **Connection String** (PostgreSQL)
- Формат: `postgresql://postgres:[PASSWORD]@[HOST]:[PORT]/postgres`
- Для использования с asyncpg добавьте `+asyncpg`: `postgresql+asyncpg://...`

---

## 🐳 Шаг 2: Настройка сервисов в Dokploy

### 2.1 Создание Backend сервиса

1. В Dokploy перейдите в **Applications** → **New Application**
2. Выберите **Docker Compose**
3. Настройки:
   - **Name:** `neiromatrius-backend` (или любое другое имя)
   - **Git Source:** Укажите ваш GitHub репозиторий
   - **Branch:** `dev` (или ваша рабочая ветка)
   - **Docker Compose File:** `dokploy/docker-compose.backend.yml`
   - **Root Directory:** `/` (корень репозитория)
   
**Важно:** 
- Dokploy автоматически создаст контейнер и сеть
- Не указывайте `container_name` в docker-compose файле - Dokploy сам создаст имя
- Для связи между сервисами используйте имя сервиса (например, `backend`, `redis`)

### 2.2 Создание Frontend сервиса

1. Создайте новый **Docker Compose** сервис
2. Настройки:
   - **Name:** `neiromatrius-frontend` (или любое другое имя)
   - **Git Source:** Тот же репозиторий
   - **Branch:** `dev` (или ваша рабочая ветка)
   - **Docker Compose File:** `dokploy/docker-compose.frontend.yml`
   - **Root Directory:** `/` (корень репозитория)
   
**Важно:**
- Убедитесь, что `BACKEND_API_URL` указывает на правильный сервис
- Если backend в том же Dokploy проекте, используйте имя сервиса: `http://backend:8000`
- Если backend на другом домене, используйте полный URL

### 2.3 Создание Redis сервиса (рекомендуется для production)

**Важно:** Redis не является встроенным template Dokploy. Мы разворачиваем его как отдельный Docker Compose сервис через Git репозиторий.

**Шаги:**

1. В Dokploy создайте новый **Docker Compose** сервис:
   - **Name:** `neiromatrius-redis` (или любое другое имя)
   - **Git Source:** Укажите ваш GitHub репозиторий (тот же, что для backend/frontend)
   - **Branch:** `dev` (или ваша рабочая ветка)
   - **Docker Compose File:** `dokploy/docker-compose.redis.yml`
   - **Root Directory:** `/` (корень репозитория)

2. После создания сервиса нажмите **Deploy**

3. Дождитесь успешного деплоя Redis

4. **Настройте REDIS_URL в Backend:**
   - Если Redis в том же Dokploy проекте: `REDIS_URL=redis://redis:6379/0`
   - Если Redis на другом сервисе: используйте полный URL

**Примечание:** 
- Redis опционален - система будет работать с in-memory cache, но для production с несколькими инстансами backend Redis необходим для предотвращения дубликатов сообщений
- Redis разворачивается из того же Git репозитория, что и остальные сервисы
- Это не встроенный template Dokploy, а наш собственный Docker Compose файл
- Для связи между сервисами используйте имя сервиса (например, `redis` вместо `neiromatrius-redis`)

---

## 🔐 Шаг 3: Настройка переменных окружения

### 3.1 Backend переменные

В Dokploy для сервиса `neiromatrius-backend` добавьте переменные из `dokploy/DOKPLOY_ENV_VARIABLES.md` (секция Backend).

**Важно для Redis:**
- Если создали Redis сервис: `REDIS_URL=redis://neiromatrius-redis:6379/0`
- Если используете внешний Redis: укажите его URL
- Если не используете Redis: оставьте пустым (не рекомендуется для production)

**Обязательные переменные:**
- `DATABASE_URL` - строка подключения к Supabase
- `OPENAI_API_KEY` - ключ OpenAI API
- `SECRET_KEY` - секретный ключ приложения

**Рекомендуется хранить в Secrets:**
- `SECRET_KEY`
- `DATABASE_URL`
- `OPENAI_API_KEY`
- `TELEGRAM_BOT_TOKEN` (если используется)

### 3.2 Frontend переменные

Для сервиса `neiromatrius-frontend` добавьте:
- `NEXT_PUBLIC_API_URL` - URL API (обычно `/api` если используется Traefik)
- `BACKEND_API_URL` - внутренний URL backend (например, `http://neiromatrius-backend:8000`)

---

## 🌐 Шаг 4: Настройка доменов и маршрутизации

### 4.1 Backend домен

1. В Dokploy для backend сервиса:
   - Перейдите в **Domains**
   - Добавьте домен (например: `api.your-domain.com`)
   - Настройте SSL (Let's Encrypt)

### 4.2 Frontend домен

1. Для frontend сервиса:
   - Добавьте домен (например: `admin.your-domain.com`)
   - Настройте SSL

### 4.3 Traefik маршрутизация (если используется)

Если используете Traefik в Dokploy, настройте маршруты:
- `/api/*` → `neiromatrius-backend:8000`
- `/*` → `neiromatrius-frontend:3000`

---

## 🔄 Шаг 5: Деплой

### 5.1 Первый деплой

1. В Dokploy нажмите **Deploy** для каждого сервиса
2. Следите за логами деплоя
3. Проверьте health checks:
   - Backend: `https://api.your-domain.com/health`
   - Frontend: `https://admin.your-domain.com`

### 5.2 Автоматический деплой из GitHub

Dokploy автоматически деплоит при push в указанную ветку, если настроен webhook.

**Проверка webhook:**
1. В Dokploy → **Git Sources** → ваш репозиторий
2. Убедитесь, что webhook настроен в GitHub
3. При push в `main` должен запуститься автоматический деплой

---

## ✅ Шаг 6: Проверка работоспособности

### 6.1 Проверка Backend

```bash
# Health check
curl https://api.your-domain.com/health

# Проверка API
curl https://api.your-domain.com/api/messages/ \
  -X POST \
  -H "Content-Type: application/json" \
  -H "X-Webhook-URL: https://test-webhook.com/test" \
  -d '{
    "client_id": "test_client_001",
    "content": "Привет! Тестовое сообщение"
  }'
```

### 6.2 Проверка Frontend

1. Откройте `https://admin.your-domain.com`
2. Проверьте подключение к API
3. Проверьте функциональность интерфейса

---

## 🔧 Шаг 7: Настройка Telegram бота (опционально)

Если используете Telegram интеграцию:

1. Получите токен у @BotFather
2. Добавьте в переменные backend:
   ```bash
   TELEGRAM_BOT_TOKEN=your_token_here
   TELEGRAM_ENABLED=true
   TELEGRAM_WEBHOOK_URL=https://api.your-domain.com/api/integrations/telegram/webhook
   ```
3. Перезапустите backend сервис

---

## 📊 Мониторинг и логи

### Просмотр логов

В Dokploy для каждого сервиса:
- **Logs** → просмотр логов в реальном времени
- **Metrics** → метрики использования ресурсов

### Health Checks

Оба сервиса имеют настроенные health checks:
- Backend: `/health` endpoint
- Frontend: HTTP check на порту 3000

---

## 🐛 Решение проблем

### Backend не запускается

1. Проверьте логи в Dokploy
2. Убедитесь, что `DATABASE_URL` правильный
3. Проверьте, что миграции выполнены в Supabase
4. Убедитесь, что все обязательные переменные установлены

### Frontend не подключается к Backend

1. Проверьте `BACKEND_API_URL` и `NEXT_PUBLIC_API_URL`
2. Убедитесь, что оба сервиса в одной сети Docker
3. Проверьте CORS настройки в backend (`ALLOWED_ORIGINS`)

### База данных не подключается

1. Проверьте `DATABASE_URL` формат (должен содержать `+asyncpg`)
2. Убедитесь, что Supabase доступен из Dokploy сервера
3. Проверьте firewall правила

---

## 📚 Дополнительные ресурсы

- [Dokploy Documentation](https://docs.dokploy.com/docs/core)
- [Supabase Self-Hosted Guide](https://supabase.com/docs/guides/self-hosting)
- [Docker Compose в Dokploy](https://docs.dokploy.com/docs/core/docker-compose)

---

## 📝 Чеклист перед деплоем

- [ ] База данных Supabase настроена
- [ ] Миграции выполнены (`database/migrations_supabase.sql`)
- [ ] GitHub репозиторий готов
- [ ] Переменные окружения настроены в Dokploy
- [ ] Домены настроены и SSL активен
- [ ] Health checks проходят
- [ ] Логи проверены на ошибки
- [ ] Telegram бот настроен (если используется)

---

**Дата создания:** 8 декабря 2025  
**Версия:** 1.0


# ✅ Функция execute_sql создана - Следующие шаги

**Дата:** 8 декабря 2025

---

## ✅ Что уже сделано

1. ✅ Функция `execute_sql` создана через Supabase SQL Editor
2. ✅ Конфигурация MCP обновлена (убран `--db-url`)
3. ✅ Права на выполнение функции предоставлены

---

## 🔄 Шаг 1: Перезапустите Cursor

**Важно:** MCP сервер проверяет наличие функции `execute_sql` только при инициализации (при запуске Cursor).

1. **Полностью закройте Cursor** (не просто перезагрузите окно)
2. **Запустите Cursor снова**
3. **Дождитесь инициализации MCP сервера** (обычно несколько секунд)

---

## ✅ Шаг 2: Проверьте работу MCP

После перезапуска Cursor попробуйте выполнить команды:

### Проверка 1: Список таблиц
```bash
# В Cursor через MCP инструменты
list_tables
```

**Ожидаемый результат:** Список таблиц в базе данных

### Проверка 2: Выполнение SQL
```bash
# В Cursor через MCP инструменты
execute_sql: SELECT table_name FROM information_schema.tables WHERE table_schema = 'public' LIMIT 5;
```

**Ожидаемый результат:** JSON с результатами запроса

---

## 🗄️ Шаг 3: Выполните миграции через MCP

После успешной проверки можно выполнить миграции:

### Вариант 1: Через execute_sql (по частям)

Выполните миграции по секциям:

1. **Создание ENUM типов:**
   ```sql
   -- Скопируйте секцию 1 из database/migrations_supabase.sql
   -- Выполните через execute_sql
   ```

2. **Создание таблиц:**
   ```sql
   -- Скопируйте секцию 2 из database/migrations_supabase.sql
   -- Выполните через execute_sql
   ```

3. **Создание индексов:**
   ```sql
   -- Скопируйте секцию 3 из database/migrations_supabase.sql
   -- Выполните через execute_sql
   ```

4. **Создание функций и триггеров:**
   ```sql
   -- Скопируйте секцию 5 из database/migrations_supabase.sql
   -- Выполните через execute_sql
   ```

### Вариант 2: Через Supabase SQL Editor (рекомендуется)

Для миграций рекомендуется использовать **Supabase SQL Editor**:

1. Откройте Supabase SQL Editor
2. Скопируйте весь файл `database/migrations_supabase.sql`
3. Выполните SQL код
4. Проверьте результат

**Преимущества:**
- ✅ Выполнение всего скрипта за раз
- ✅ Транзакции работают корректно
- ✅ Легче отслеживать ошибки

---

## 🔍 Проверка после миграций

После выполнения миграций проверьте созданные таблицы:

```sql
SELECT table_name 
FROM information_schema.tables 
WHERE table_schema = 'public' 
AND table_name IN (
    'messages', 'classifications', 'response_templates', 'keywords',
    'operator_feedback', 'operator_session_logs', 'reminders',
    'chat_sessions', 'operator_message_reads'
)
ORDER BY table_name;
```

**Ожидаемый результат:** 9 таблиц

Проверка ENUM типов:

```sql
SELECT typname 
FROM pg_type 
WHERE typtype = 'e' 
AND typname IN (
    'messagetype', 'scenariotype', 'remindertype', 'dialogstatus',
    'prioritylevel', 'escalationreason'
)
ORDER BY typname;
```

**Ожидаемый результат:** 6 ENUM типов

---

## ⚠️ Если MCP не работает после перезапуска

1. **Проверьте логи MCP сервера:**
   - В Cursor откройте MCP Logs
   - Ищите сообщения об ошибках

2. **Проверьте функцию вручную:**
   ```sql
   -- В Supabase SQL Editor
   SELECT proname, proargtypes 
   FROM pg_proc 
   WHERE proname = 'execute_sql';
   ```
   
   **Ожидаемый результат:** Одна строка с функцией `execute_sql`

3. **Проверьте права:**
   ```sql
   -- В Supabase SQL Editor
   SELECT grantee, privilege_type 
   FROM information_schema.routine_privileges 
   WHERE routine_name = 'execute_sql';
   ```
   
   **Ожидаемый результат:** Права для `authenticated`, `anon`, `service_role`

---

## 📋 Чеклист

- [ ] Функция `execute_sql` создана
- [ ] Cursor перезапущен
- [ ] MCP команда `list_tables` работает
- [ ] MCP команда `execute_sql` работает
- [ ] Миграции выполнены (через SQL Editor или MCP)
- [ ] Таблицы созданы и проверены
- [ ] ENUM типы созданы и проверены

---

## 🎯 Рекомендация

**Для миграций:** Используйте **Supabase SQL Editor** - это самый надежный способ.

**Для работы с данными:** Используйте **MCP через REST API** - удобно для запросов и операций с данными.

---

**Дата:** 8 декабря 2025







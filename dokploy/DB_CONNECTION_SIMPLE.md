# 🗄️ Простое подключение к Supabase (как в примере заказчика)

**Проект:** Neiromatrius  
**Дата:** 8 декабря 2025

---

## ✅ Решение

**Заказчик хотел подключение через ПАРАМЕТРЫ (как в примере), а не через REST API!**

Система теперь работает точно так же, как в примере заказчика.

---

## 📋 Настройка в Dokploy

### Обязательные переменные:

```bash
# Имя СЕРВИСА БД внутри Docker сети (НЕ внешний хост!)
# Найдите в Supabase Service → Containers → контейнер с PostgreSQL
# Обычно это "db" или "supabase-db"
SUPABASE_HOST=db

# Порт БД (обычно 5432)
SUPABASE_PORT=5432

# Пользователь БД (обычно "postgres")
SUPABASE_USER=postgres

# Пароль БД - ⚠️ ВАЖНО: из POSTGRES_PASSWORD в Supabase Service!
SUPABASE_PASSWORD=your_db_password

# Имя базы данных - ⚠️ ВАЖНО: из POSTGRES_DB в Supabase Service!
SUPABASE_DB=postgres

# Логирование SQL запросов (true/false)
DATABASE_ECHO=false
```

### Где взять параметры:

1. **В Dokploy** → **Supabase Service** → **Environment Variables**
2. `POSTGRES_PASSWORD` → `SUPABASE_PASSWORD`
3. `POSTGRES_DB` → `SUPABASE_DB`
4. Имя контейнера PostgreSQL → `SUPABASE_HOST` (обычно "db")

### ⚠️ Важно:

- `SUPABASE_HOST` - это имя **сервиса** в Docker сети, НЕ внешний домен!
- Например: `db`, `supabase-db`, но НЕ `supabase.neiroaleksandra.dev.zerocoder.pro`

---

## 🔧 Как это работает

Система автоматически строит `DATABASE_URL` из параметров:

```
postgresql+asyncpg://postgres:password@db:5432/postgres
```

**Если `DATABASE_URL` указан напрямую** - используется он.  
**Иначе** - строится из параметров Supabase.

---

## ✅ Результат

- ✅ **НЕ требуется** переработка сервисов
- ✅ **НЕ требуется** замена SQLAlchemy на REST API
- ✅ Просто правильная конфигурация параметров подключения
- ✅ Всё работает как раньше!

---

**Дата:** 8 декабря 2025








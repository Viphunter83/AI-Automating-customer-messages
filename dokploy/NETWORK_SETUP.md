# 🌐 Настройка сети Docker в Dokploy

**Проект:** Neiromatrius  
**Дата:** 8 декабря 2025

---

## 🔍 Важно: Сеть Docker

Все сервисы должны быть в **одной Docker сети** для взаимодействия между собой.

---

## 📋 Настройка сети в Dokploy

### Вариант 1: Автоматическое создание сети (рекомендуется)

Dokploy автоматически создаст сеть при первом деплое сервиса.

**Шаги:**

1. **При создании первого сервиса (например, Backend):**
   - Dokploy автоматически создаст сеть `neiromatrius-network`
   - Или использует имя по умолчанию

2. **Для последующих сервисов:**
   - Укажите ту же сеть: `neiromatrius-network`
   - Или используйте автоматическую сеть Dokploy

### Вариант 2: Ручное создание сети

Если нужно создать сеть вручную:

```bash
# В Dokploy через терминал или CLI
docker network create neiromatrius-network
```

---

## 🔧 Конфигурация в Docker Compose файлах

Все файлы используют:

```yaml
networks:
  neiromatrius-network:
    external: true
```

**Это означает:**
- Сеть должна существовать до деплоя сервиса
- Или Dokploy создаст ее автоматически при первом деплое

---

## 📝 Имена сервисов для связи

### Внутри Docker сети

Сервисы могут обращаться друг к другу по **имени контейнера**:

- Backend: `neiromatrius-backend:8000`
- Frontend: `neiromatrius-frontend:3000`
- Redis: `neiromatrius-redis:6379`

### Примеры использования

**В Frontend для связи с Backend:**
```bash
BACKEND_API_URL=http://neiromatrius-backend:8000
```

**В Backend для связи с Redis:**
```bash
REDIS_URL=redis://neiromatrius-redis:6379/0
```

---

## ✅ Проверка сети

После деплоя всех сервисов проверьте:

```bash
# В Dokploy через терминал
docker network inspect neiromatrius-network

# Должны быть видны все контейнеры:
# - neiromatrius-backend
# - neiromatrius-frontend
# - neiromatrius-redis (если используется)
```

---

## 🐛 Решение проблем

### Проблема: Сервисы не могут связаться друг с другом

**Решение:**
1. Убедитесь, что все сервисы в одной сети
2. Проверьте имя сети в docker-compose файлах
3. Убедитесь, что сеть создана (`external: true`)

### Проблема: Сеть не найдена

**Решение:**
1. Создайте сеть вручную или дайте Dokploy создать автоматически
2. Убедитесь, что `external: true` указан правильно
3. Проверьте имя сети в каждом docker-compose файле

---

**Дата создания:** 8 декабря 2025








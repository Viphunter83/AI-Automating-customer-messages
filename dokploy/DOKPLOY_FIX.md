# 🔧 Исправление ошибки "No such container: select-a-container"

**Проблема:** Ошибка возникает в Dokploy после деплоя из Git репозитория.

**Причина:** Dokploy не поддерживает `container_name` и `external: true` для сетей в Docker Compose файлах.

---

## ✅ Решение

### 1. Обновлены Docker Compose файлы

Все файлы в `dokploy/` обновлены:
- ✅ Удален `container_name` - Dokploy автоматически создаст имя контейнера
- ✅ Удалены `networks` с `external: true` - Dokploy использует default сеть автоматически
- ✅ Обновлены URL для связи между сервисами (используется имя сервиса вместо container_name)

### 2. Изменения в файлах

**`docker-compose.backend.yml`:**
- Удален `container_name: neiromatrius-backend`
- Удалена секция `networks` с `external: true`

**`docker-compose.frontend.yml`:**
- Удален `container_name: neiromatrius-frontend`
- Удалена секция `networks` с `external: true`
- Обновлен `BACKEND_API_URL` на использование имени сервиса: `http://backend:8000`

**`docker-compose.redis.yml`:**
- Удален `container_name: neiromatrius-redis`
- Удалена секция `networks` с `external: true`

### 3. Обновление переменных окружения

**Для Backend (REDIS_URL):**
```bash
# Если Redis в том же Dokploy проекте:
REDIS_URL=redis://redis:6379/0

# Если Redis на другом сервисе:
REDIS_URL=redis://your-redis-host:6379/0
```

**Для Frontend (BACKEND_API_URL):**
```bash
# Если Backend в том же Dokploy проекте:
BACKEND_API_URL=http://backend:8000

# Если Backend на другом домене:
BACKEND_API_URL=https://your-backend-domain.com
```

---

## 🚀 Действия после исправления

1. **Закоммитьте изменения в Git:**
   ```bash
   git add dokploy/
   git commit -m "Fix: Remove container_name and external networks for Dokploy compatibility"
   git push origin dev
   ```

2. **В Dokploy:**
   - Перейдите в каждый сервис (Backend, Frontend, Redis)
   - Нажмите **Redeploy** или **Deploy**
   - Дождитесь успешного деплоя

3. **Проверьте логи:**
   - В Dokploy → сервис → Logs
   - Должны быть логи без ошибок "No such container"

---

## 📝 Важные примечания

### Имена сервисов в Dokploy

- Dokploy использует **имя сервиса** (из docker-compose файла) для связи между контейнерами
- Например: `backend`, `frontend`, `redis` (не `neiromatrius-backend`)
- Имена контейнеров генерируются автоматически Dokploy

### Сети Docker

- Dokploy автоматически создает default сеть для всех сервисов в проекте
- Все сервисы в одном Dokploy проекте могут общаться друг с другом по имени сервиса
- Не нужно создавать внешние сети вручную

### Связь между сервисами

**Внутри одного Dokploy проекта:**
- Backend → Redis: `redis://redis:6379/0`
- Frontend → Backend: `http://backend:8000`

**Между разными Dokploy проектами/доменами:**
- Используйте полные URL: `https://your-service-domain.com`

---

## ✅ Проверка работоспособности

После деплоя проверьте:

1. **Backend health:**
   ```bash
   curl http://your-backend-domain:8000/health
   ```

2. **Frontend доступность:**
   ```bash
   curl http://your-frontend-domain:3000
   ```

3. **Логи без ошибок:**
   - В Dokploy → каждый сервис → Logs
   - Не должно быть ошибок "No such container"

---

**Дата:** 9 декабря 2025


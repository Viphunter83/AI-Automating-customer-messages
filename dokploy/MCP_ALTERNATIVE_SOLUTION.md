# ✅ Альтернативное решение: Выполнить миграции через SQL Editor

**Дата:** 8 декабря 2025

**Проблема:** MCP сервер не видит функцию `execute_sql` даже после всех попыток.

**Решение:** Выполнить миграции напрямую через Supabase SQL Editor.

---

## 🎯 Почему SQL Editor лучше для миграций

1. ✅ **Работает всегда** - не зависит от MCP
2. ✅ **Выполнение всего скрипта за раз** - транзакции работают корректно
3. ✅ **Легче отслеживать ошибки** - видно все ошибки сразу
4. ✅ **Быстрее** - не нужно ждать инициализации MCP

---

## 📋 Шаги для выполнения миграций

### Шаг 1: Откройте Supabase SQL Editor

1. Откройте Dokploy Dashboard
2. Найдите сервис Supabase
3. Откройте SQL Editor (обычно доступен через веб-интерфейс Supabase)

**Или через браузер:**
- Откройте `http://supabase.dev.neiromatrius.zerocoder.pro:8000` (если доступен веб-интерфейс)
- Найдите раздел "SQL Editor" или "Database" → "SQL Editor"

### Шаг 2: Откройте файл миграций

Откройте файл: `database/migrations_supabase.sql`

### Шаг 3: Скопируйте весь SQL код

Скопируйте весь содержимое файла `database/migrations_supabase.sql` в буфер обмена.

### Шаг 4: Вставьте в SQL Editor

1. Вставьте SQL код в SQL Editor
2. Проверьте, что весь код вставлен корректно

### Шаг 5: Выполните SQL

1. Нажмите кнопку "Run" или "Execute" (или `Ctrl+Enter` / `Cmd+Enter`)
2. Дождитесь выполнения (может занять несколько секунд)
3. Проверьте результат

### Шаг 6: Проверьте результат

После выполнения вы должны увидеть:
- ✅ Сообщение об успешном выполнении
- ✅ Список созданных таблиц, типов, функций

**Если есть ошибки:**
- Проверьте сообщения об ошибках
- Убедитесь, что все ENUM типы созданы до создания таблиц
- Убедитесь, что все таблицы созданы до создания индексов и триггеров

---

## 🔍 Проверка после миграций

После выполнения миграций проверьте:

### 1. Проверка таблиц

```sql
SELECT table_name 
FROM information_schema.tables 
WHERE table_schema = 'public' 
ORDER BY table_name;
```

**Ожидаемые таблицы:**
- `conversations`
- `messages`
- `scenarios`
- `scenario_responses`
- `scenario_conditions`
- `reminders`
- `escalations`
- `analytics_events`
- `llm_usage`
- `system_settings`

### 2. Проверка ENUM типов

```sql
SELECT typname 
FROM pg_type 
WHERE typtype = 'e' 
ORDER BY typname;
```

**Ожидаемые типы:**
- `messagetype`
- `scenariotype`
- `remindertype`
- `escalationstatus`
- `analytics_eventtype`

### 3. Проверка функций

```sql
SELECT proname 
FROM pg_proc 
WHERE pronamespace = 'public'::regnamespace 
ORDER BY proname;
```

---

## 🎯 После выполнения миграций

После успешного выполнения миграций:

1. ✅ **База данных готова** - все таблицы, типы, функции созданы
2. ✅ **Backend может работать** - подключение к БД настроено
3. ✅ **MCP можно настроить позже** - для работы с данными (не критично для миграций)

---

## 📝 Примечания

- **MCP не критичен для миграций** - миграции можно выполнить через SQL Editor
- **MCP полезен для работы с данными** - после настройки можно использовать для запросов
- **Если MCP все еще не работает** - это не блокирует работу проекта, миграции выполнены

---

**Дата:** 8 декабря 2025



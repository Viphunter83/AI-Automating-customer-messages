# ✅ Финальные шаги для работы MCP

**Дата:** 8 декабря 2025

---

## ✅ Что уже сделано

1. ✅ Функция `execute_sql` создана в БД
2. ✅ Права на функцию предоставлены
3. ✅ Выполнена команда `NOTIFY pgrst, 'reload schema'` для перезагрузки схемы PostgREST

---

## 🔄 Шаг 1: Перезапустите Cursor

**Критически важно:** MCP сервер проверяет наличие функции `execute_sql` только при инициализации (при запуске Cursor).

После выполнения `NOTIFY pgrst, 'reload schema'` **обязательно** перезапустите Cursor:

1. **Полностью закройте Cursor** (не просто перезагрузите окно)
2. **Запустите Cursor снова**
3. **Дождитесь инициализации MCP сервера** (обычно несколько секунд)

---

## ✅ Шаг 2: Проверьте работу MCP

После перезапуска Cursor попробуйте выполнить команды:

### Проверка 1: Список таблиц
```bash
# В Cursor через MCP инструменты
list_tables
```

**Ожидаемый результат:** Список таблиц в базе данных

### Проверка 2: Выполнение SQL
```bash
# В Cursor через MCP инструменты
execute_sql: SELECT table_name FROM information_schema.tables WHERE table_schema = 'public' LIMIT 5;
```

**Ожидаемый результат:** JSON с результатами запроса

---

## 🔍 Если MCP все еще не работает

### Диагностика 1: Проверьте функцию в БД

```sql
-- В Supabase SQL Editor
SELECT 
    proname, 
    pronamespace::regnamespace as schema,
    proargtypes
FROM pg_proc 
WHERE proname = 'execute_sql';
```

**Ожидаемый результат:** 
- `proname`: `execute_sql`
- `schema`: `public`
- `proargtypes`: Должен быть массив типов

### Диагностика 2: Проверьте права на функцию

```sql
-- В Supabase SQL Editor
SELECT 
    grantee, 
    privilege_type 
FROM information_schema.routine_privileges 
WHERE routine_name = 'execute_sql'
ORDER BY grantee;
```

**Ожидаемый результат:** Права для:
- `authenticated`
- `anon`
- `service_role`

### Диагностика 3: Попробуйте вызвать функцию напрямую через SQL

```sql
-- В Supabase SQL Editor
SELECT public.execute_sql('SELECT 1 as test', true);
```

**Ожидаемый результат:** `[{"test": 1}]` или похожий JSON массив

---

## 🗄️ Шаг 3: Выполните миграции

После успешной проверки MCP можно выполнить миграции:

### Рекомендуется: Через Supabase SQL Editor

1. Откройте Supabase SQL Editor
2. Скопируйте весь файл `database/migrations_supabase.sql`
3. Выполните SQL код
4. Проверьте результат

**Преимущества:**
- ✅ Выполнение всего скрипта за раз
- ✅ Транзакции работают корректно
- ✅ Легче отслеживать ошибки

### Альтернатива: Через MCP execute_sql

Если MCP работает, можно выполнить миграции по частям через `execute_sql`.

---

## 📋 Чеклист

- [x] Функция `execute_sql` создана в БД
- [x] Права на функцию предоставлены
- [x] Выполнена команда `NOTIFY pgrst, 'reload schema'`
- [ ] **Cursor перезапущен** (критически важно!)
- [ ] MCP команда `list_tables` работает
- [ ] MCP команда `execute_sql` работает
- [ ] Миграции выполнены (через SQL Editor или MCP)
- [ ] Таблицы созданы и проверены
- [ ] ENUM типы созданы и проверены

---

## ⚠️ Важно

**MCP сервер проверяет функцию только при инициализации** (при запуске Cursor).

После выполнения `NOTIFY pgrst, 'reload schema'` **обязательно** перезапустите Cursor, иначе MCP сервер не узнает о перезагрузке схемы PostgREST.

---

## 🎯 Рекомендация

**Для миграций:** Используйте **Supabase SQL Editor** - это самый надежный способ.

**Для работы с данными:** Используйте **MCP через REST API** - удобно для запросов и операций с данными.

---

**Дата:** 8 декабря 2025






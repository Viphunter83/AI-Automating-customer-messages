# 🔧 Решение проблем при деплое

**Проект:** Neiromatrius  
**Платформа:** Dokploy

---

## ❌ Ошибка: "No such container: select-a-container"

### Причина

Эта ошибка возникает в Dokploy UI, когда вы пытаетесь выполнить команду (например, через терминал или логи), но не выбрали контейнер из списка.

### Решение

1. **В Dokploy Dashboard:**
   - Перейдите в ваш сервис (например, `neiromatrius-backend`)
   - Убедитесь, что сервис развернут (статус "Running")
   - Если сервис не запущен, нажмите **Deploy**

2. **Для просмотра логов:**
   - В Dokploy перейдите в сервис → **Logs**
   - Логи должны отображаться автоматически

3. **Для выполнения команд:**
   - В Dokploy перейдите в сервис → **Terminal**
   - Убедитесь, что выбран контейнер из выпадающего списка
   - Если список пуст, сначала выполните **Deploy**

---

## ❌ Ошибка: "Network neiromatrius-network not found"

### Причина

Docker сеть не создана перед запуском сервисов.

### Решение

**Вариант 1: Создать сеть вручную (рекомендуется)**

В Dokploy создайте сеть через терминал или добавьте в docker-compose файл:

```yaml
networks:
  neiromatrius-network:
    driver: bridge
```

**Вариант 2: Использовать существующую сеть**

В Dokploy обычно есть default сеть. Измените в `docker-compose.backend.yml`:

```yaml
networks:
  - default  # вместо neiromatrius-network
```

И удалите секцию:
```yaml
networks:
  neiromatrius-network:
    external: true
```

---

## ❌ Ошибка: "Cannot connect to database"

### Причина

Неправильные настройки подключения к Supabase.

### Решение

1. **Проверьте переменные окружения в Dokploy:**
   ```bash
   SUPABASE_USER=postgres
   SUPABASE_PASSWORD=your-password  # ⚠️ ОБЯЗАТЕЛЬНО для внешнего Supabase!
   SUPABASE_HOST=supabase.dev.neiromatrius.zerocoder.pro
   SUPABASE_PORT=5437
   SUPABASE_DB=postgres
   ```

2. **Проверьте доступность Supabase:**
   ```bash
   # Из контейнера backend попробуйте:
   ping supabase.dev.neiromatrius.zerocoder.pro
   ```

3. **Проверьте логи backend:**
   - В Dokploy → сервис → Logs
   - Ищите ошибки подключения к БД

---

## ❌ Ошибка: "Redis connection failed"

### Причина

Redis сервис не запущен или неправильный URL.

### Решение

1. **Если Redis не используется:**
   - Оставьте `REDIS_URL` пустым
   - Система будет использовать in-memory cache

2. **Если Redis используется:**
   - Убедитесь, что Redis сервис развернут
   - Проверьте `REDIS_URL`:
     ```bash
     REDIS_URL=redis://neiromatrius-redis:6379/0
     ```
   - Проверьте, что оба сервиса в одной сети

---

## ❌ Ошибка: "Frontend не загружается"

### Причина

Frontend контейнер остановлен или неправильные настройки.

### Решение

1. **Проверьте статус контейнера:**
   ```bash
   docker ps -a | grep frontend
   ```

2. **Перезапустите frontend:**
   - В Dokploy → сервис `neiromatrius-frontend` → **Restart**

3. **Проверьте переменные окружения:**
   ```bash
   NEXT_PUBLIC_API_URL=/api
   BACKEND_API_URL=http://neiromatrius-backend:8000
   ```

4. **Проверьте логи:**
   - В Dokploy → сервис → Logs
   - Ищите ошибки сборки или запуска

---

## ✅ Проверка работоспособности

### 1. Проверка Backend

```bash
# Health check
curl http://your-backend-domain:8000/health

# Должен вернуть:
# {"status":"healthy","version":"1.0.0"}
```

### 2. Проверка Frontend

```bash
# Откройте в браузере:
http://your-frontend-domain:3000

# Должна загрузиться страница оператора
```

### 3. Проверка подключения к БД

В логах backend должны быть сообщения:
```
✅ Database migrations should be executed via Supabase SQL Editor
🚀 Starting Neiromatrius Backend...
```

Если есть ошибки подключения, они будут в логах.

---

## 📞 Дополнительная помощь

Если проблема не решена:

1. **Соберите информацию:**
   - Логи всех сервисов
   - Статус контейнеров (`docker ps -a`)
   - Переменные окружения (без секретов!)

2. **Проверьте документацию:**
   - `DEPLOYMENT_GUIDE.md` - полное руководство по деплою
   - `BACKEND_ENV_VARIABLES.md` - переменные окружения для backend
   - `DOKPLOY_ENV_VARIABLES.md` - все переменные окружения

---

**Дата:** 9 декабря 2025


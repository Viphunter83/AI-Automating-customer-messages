# ⚡ Быстрый старт: Redis в Dokploy

**Проект:** Neiromatrius  
**Дата:** 8 декабря 2025

---

## 🎯 Краткий ответ

**Redis разворачивается через Git репозиторий как отдельный Docker Compose сервис, а не через встроенные templates Dokploy.**

---

## 📋 Пошаговая инструкция

### Шаг 1: Убедитесь, что файл в репозитории

Файл `dokploy/docker-compose.redis.yml` должен быть в вашем GitHub репозитории.

### Шаг 2: Создайте Redis сервис в Dokploy

1. В Dokploy: **Applications** → **New Application**
2. Выберите **Docker Compose**
3. Заполните:
   ```
   Name: neiromatrius-redis
   Git Source: [ваш GitHub репозиторий]
   Branch: main
   Docker Compose File: dokploy/docker-compose.redis.yml
   Root Directory: /
   ```

### Шаг 3: Задеплойте

1. Нажмите **Deploy**
2. Дождитесь успешного деплоя
3. Проверьте логи - должно быть: `Ready to accept connections`

### Шаг 4: Настройте backend

В backend сервисе добавьте переменную окружения:
```bash
REDIS_URL=redis://neiromatrius-redis:6379/0
```

⚠️ **Важно:** Используйте имя контейнера `neiromatrius-redis`!

### Шаг 5: Перезапустите backend

1. В Dokploy для backend сервиса: **Restart**
2. Проверьте логи - должно быть: `✅ Redis cache connected`

---

## ✅ Проверка

```bash
# Health check Redis
curl http://your-backend-domain/api/monitoring/stats

# Должно быть в ответе:
{
  "cache": {
    "status": "redis_connected",
    "type": "redis"
  }
}
```

---

## 🔄 Порядок деплоя сервисов

1. **Redis** (если используете)
2. **Backend**
3. **Frontend**

Backend должен запускаться после Redis, чтобы подключиться к нему.

---

## 📚 Подробности

См. `REDIS_SETUP.md` для полной документации.

---

**Дата:** 8 декабря 2025








# 🔧 Исправление подключения MCP к Supabase БД

**Проблема:** `Failed to connect pg pool: Tenant or user not found (XX000)`

---

## 🔍 Причина ошибки

Ошибка "Tenant or user not found" возникает когда:
1. **Подключение идет через Supabase Gateway**, а не напрямую к PostgreSQL
2. **Неправильный порт** - порт 5437 может быть прокси, а не прямой PostgreSQL порт
3. **Нужен прямой доступ к PostgreSQL** на стандартном порту 5432

---

## ✅ Решение 1: Использовать правильный порт PostgreSQL

Для self-hosted Supabase стандартный порт PostgreSQL - **5432**, а не 5437.

**Проверьте в Dokploy:**
1. Откройте Supabase Service
2. Найдите контейнер PostgreSQL
3. Проверьте, какой порт используется для PostgreSQL (обычно 5432)

**Обновите `.cursor/mcp.json`:**
```json
{
  "mcpServers": {
    "selfhosted-supabase-neiromatrius": {
      "command": "node",
      "args": [
        "/Users/apple/AI Automating customer messages /mcp-servers/selfhosted-supabase-mcp/dist/index.js",
        "--url",
        "http://supabase.dev.neiromatrius.zerocoder.pro:8000",
        "--anon-key",
        "YOUR_ANON_KEY",
        "--service-key",
        "YOUR_SERVICE_KEY",
        "--db-url",
        "postgresql://postgres:PASSWORD@supabase.dev.neiromatrius.zerocoder.pro:5432/postgres",
        "--jwt-secret",
        "YOUR_JWT_SECRET"
      ]
    }
  }
}
```

**Изменения:**
- Порт изменен с `5437` на `5432` (стандартный PostgreSQL порт)

---

## ✅ Решение 2: Использовать внутренний хост (если доступен)

Если Supabase развернут в Dokploy, используйте внутренний хост:

**Обновите `.cursor/mcp.json`:**
```json
{
  "mcpServers": {
    "selfhosted-supabase-neiromatrius": {
      "command": "node",
      "args": [
        "/Users/apple/AI Automating customer messages /mcp-servers/selfhosted-supabase-mcp/dist/index.js",
        "--url",
        "http://supabase.dev.neiromatrius.zerocoder.pro:8000",
        "--anon-key",
        "YOUR_ANON_KEY",
        "--service-key",
        "YOUR_SERVICE_KEY",
        "--db-url",
        "postgresql://postgres:PASSWORD@db:5432/postgres",
        "--jwt-secret",
        "YOUR_JWT_SECRET"
      ]
    }
  }
}
```

**Изменения:**
- Хост изменен на `db` (внутренний хост в Docker сети)
- Порт `5432` (стандартный PostgreSQL порт)

**⚠️ Внимание:** Это работает только если MCP запускается в той же Docker сети, что и Supabase.

---

## ✅ Решение 3: Проверить правильность порта в Dokploy

1. **Откройте Dokploy Dashboard**
2. **Найдите Supabase Service**
3. **Проверьте порты:**
   - Найдите контейнер PostgreSQL
   - Проверьте маппинг портов (обычно `5432:5432` или `5432:5437`)
   - Используйте **внутренний порт** (обычно 5432)

4. **Проверьте переменные окружения Supabase Service:**
   - `POSTGRES_PORT` - обычно 5432
   - `POSTGRES_DB` - обычно postgres
   - `POSTGRES_USER` - обычно postgres
   - `POSTGRES_PASSWORD` - ваш пароль

---

## ✅ Решение 4: Использовать прямой доступ через SSH туннель (если нужно)

Если прямой доступ к PostgreSQL недоступен, можно использовать SSH туннель:

```bash
# Создать SSH туннель
ssh -L 5432:localhost:5432 user@supabase.dev.neiromatrius.zerocoder.pro

# Затем использовать localhost в DATABASE_URL
postgresql://postgres:PASSWORD@localhost:5432/postgres
```

---

## 🧪 Тестирование подключения

После обновления конфигурации:

1. **Перезапустите Cursor**
2. **Попробуйте выполнить MCP команду:**
   ```bash
   # Через MCP инструменты в Cursor
   list_tables
   ```

3. **Проверьте логи MCP сервера:**
   - Откройте Cursor → MCP Logs
   - Ищите сообщения об ошибках подключения

---

## 📋 Чеклист для диагностики

- [ ] Проверен правильный порт PostgreSQL (обычно 5432)
- [ ] Проверен правильный хост (внутренний или внешний)
- [ ] Проверены учетные данные (пользователь, пароль)
- [ ] Проверена доступность порта (не заблокирован firewall)
- [ ] Проверены логи MCP сервера на наличие ошибок

---

## 🔗 Полезные ссылки

- [Supabase Self-Hosting Docs](https://supabase.com/docs/guides/self-hosting)
- [PostgreSQL Connection Strings](https://www.postgresql.org/docs/current/libpq-connect.html#LIBPQ-CONNSTRING)

---

**Дата:** 8 декабря 2025







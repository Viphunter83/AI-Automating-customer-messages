# 🔴 Настройка Redis для Neiromatrius

**Проект:** Neiromatrius  
**Дата:** 8 декабря 2025

---

## 📋 Зачем нужен Redis?

Redis используется для:

1. **Предотвращение дубликатов сообщений** - критично при нескольких инстансах backend
2. **Распределенное кеширование** - кеш классификаций AI между инстансами
3. **Метрики и статистика** - отслеживание дубликатов и производительности
4. **Улучшение производительности** - быстрый доступ к часто используемым данным

---

## ⚠️ Важно

**Redis опционален**, но **рекомендуется для production**:

- ✅ **С Redis:** Работает корректно с несколькими инстансами backend
- ⚠️ **Без Redis:** Работает, но возможны дубликаты сообщений при масштабировании

---

## 🚀 Варианты настройки

### Вариант 1: Отдельный Redis сервис в Dokploy (рекомендуется)

**Важно:** Redis разворачивается через Git репозиторий как Docker Compose сервис, а не через встроенные templates Dokploy.

**Шаги:**

1. **Убедитесь, что файл `dokploy/docker-compose.redis.yml` есть в вашем Git репозитории**

2. **В Dokploy создайте новый Docker Compose сервис:**
   - Перейдите в **Applications** → **New Application**
   - Выберите **Docker Compose**
   - **Name:** `neiromatrius-redis`
   - **Git Source:** Укажите ваш GitHub репозиторий (тот же, что для backend/frontend)
   - **Branch:** `main` (или ваша рабочая ветка)
   - **Docker Compose File:** `dokploy/docker-compose.redis.yml`
   - **Root Directory:** `/` (корень репозитория)

3. **Задеплойте Redis сервис:**
   - Нажмите **Deploy**
   - Дождитесь успешного деплоя
   - Проверьте логи - должно быть: `Ready to accept connections`

4. **В backend сервисе добавьте переменную:**
   ```bash
   REDIS_URL=redis://neiromatrius-redis:6379/0
   ```
   
   ⚠️ **Важно:** Используйте имя контейнера `neiromatrius-redis`, а не `redis`!

5. **Перезапустите backend сервис:**
   - В Dokploy для backend сервиса нажмите **Restart**
   - Проверьте логи - должно быть: `✅ Redis cache connected`

**Преимущества:**
- ✅ Полный контроль
- ✅ Бесплатно (если на своем сервере)
- ✅ Простая настройка
- ✅ Данные сохраняются в Docker volume

---

### Вариант 2: Внешний Redis сервис

**Популярные провайдеры:**
- **Redis Cloud:** https://redis.com/cloud/
- **Upstash:** https://upstash.com/
- **AWS ElastiCache:** https://aws.amazon.com/elasticache/
- **DigitalOcean Managed Redis:** https://www.digitalocean.com/products/managed-databases

**Настройка:**

1. Создайте Redis инстанс у провайдера
2. Получите connection string
3. В backend сервисе добавьте:
   ```bash
   REDIS_URL=redis://:password@your-redis-host:6379/0
   ```

**Преимущества:**
- ✅ Управляемый сервис
- ✅ Автоматические бэкапы
- ✅ Высокая доступность
- ✅ Масштабирование

---

### Вариант 3: Без Redis (только для разработки/тестирования)

**Настройка:**

1. В backend сервисе оставьте переменную пустой:
   ```bash
   REDIS_URL=
   ```
   Или не добавляйте переменную вообще

2. Система автоматически использует in-memory cache

**Ограничения:**
- ⚠️ Кеш не сохраняется между перезапусками
- ⚠️ При нескольких инстансах backend возможны дубликаты сообщений
- ⚠️ Метрики дубликатов не работают корректно

**Когда использовать:**
- ✅ Локальная разработка
- ✅ Тестирование
- ✅ Один инстанс backend
- ❌ **НЕ рекомендуется для production с несколькими инстансами**

---

## 🔧 Проверка работы Redis

### После настройки проверьте:

1. **Логи backend:**
   ```
   ✅ Redis cache connected
   ```

2. **Health check:**
   ```bash
   curl https://api.your-domain.com/api/monitoring/stats
   ```
   
   В ответе должно быть:
   ```json
   {
     "cache": {
       "status": "redis_connected",
       "type": "redis"
     }
   }
   ```

3. **Тест кеширования:**
   - Отправьте одинаковое сообщение дважды
   - Второй раз должно быть быстрее (кеш сработал)

---

## 📊 Мониторинг Redis

### Метрики в Dokploy:

- **Memory Usage:** Должно быть < 256MB (настроено в docker-compose.redis.yml)
- **Connections:** Количество подключений от backend
- **Commands:** Количество команд в секунду

### Логи Redis:

В Dokploy для Redis сервиса можно посмотреть логи для диагностики проблем.

---

## 🐛 Решение проблем

### Redis не подключается

1. **Проверьте переменную `REDIS_URL`:**
   - Формат: `redis://host:port/db`
   - Для Docker network: `redis://neiromatrius-redis:6379/0`
   - Для внешнего: `redis://:password@host:6379/0`

2. **Проверьте сеть Docker:**
   - Оба сервиса должны быть в одной сети `neiromatrius-network`

3. **Проверьте логи backend:**
   - Должно быть: `✅ Redis cache connected`
   - Если ошибка: `⚠️ Redis connection failed` - проверьте URL и доступность

### Redis использует много памяти

1. **Проверьте настройки в docker-compose.redis.yml:**
   ```yaml
   command: redis-server --maxmemory 256mb --maxmemory-policy allkeys-lru
   ```

2. **Увеличьте лимит памяти** если нужно:
   ```yaml
   command: redis-server --maxmemory 512mb --maxmemory-policy allkeys-lru
   ```

---

## 📚 Дополнительная информация

- [Redis Documentation](https://redis.io/docs/)
- [Dokploy Docker Compose](https://docs.dokploy.com/docs/core/docker-compose)
- [Redis Best Practices](https://redis.io/docs/manual/patterns/)

---

**Дата создания:** 8 декабря 2025


# ⚡ Быстрый деплой на Vercel

**Дата:** 2025-11-26

---

## ✅ Конфигурация готова!

Все файлы для деплоя созданы:
- ✅ `vercel.json` - конфигурация проекта
- ✅ `api/index.py` - serverless функция
- ✅ `requirements.txt` - зависимости Python
- ✅ `.vercelignore` - исключения

---

## 🚀 Команды для деплоя

### Шаг 1: Инициализация проекта (первый раз)

```bash
vercel
```

Следуйте инструкциям:
- Выберите команду: `team_ckk1yHJodr9A9k87ScdWHwmQ` (Oleg's projects)
- Или создайте новый проект
- Подтвердите настройки

### Шаг 2: Настройка переменных окружения

**ВАЖНО:** Перед деплоем настройте переменные окружения!

Через Dashboard:
1. Откройте проект в Vercel Dashboard
2. Settings → Environment Variables
3. Добавьте переменные из `VERCEL_ENV_VARS.md`

Или через CLI:
```bash
vercel env add DATABASE_URL production
vercel env add OPENAI_API_KEY production  
vercel env add SECRET_KEY production
```

### Шаг 3: Деплой

```bash
# Preview deployment
vercel

# Production deployment
vercel --prod
```

---

## ⚠️ КРИТИЧНО: База данных

**Vercel НЕ поддерживает локальную БД!**

Необходимо использовать внешнюю БД:
- **Supabase** (рекомендуется) - бесплатный план
- **Neon** - serverless PostgreSQL
- **Railway** - простой деплой

Обновите `DATABASE_URL` на внешнюю БД!

---

## 📝 Минимальные переменные для первого деплоя

```bash
DATABASE_URL=postgresql+asyncpg://user:pass@host:5432/db
OPENAI_API_KEY=sk-...
SECRET_KEY=your-secret-key-32-chars-minimum
```

---

## ✅ После деплоя

Проверьте:
```bash
curl https://your-project.vercel.app/health
curl https://your-project.vercel.app/docs
```

---

**Готово к деплою!** 🚀


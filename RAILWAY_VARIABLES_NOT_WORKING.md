# 🚨 Проблема: Railway не видит переменные после добавления

**Дата:** 2025-11-27  
**Проблема:** Переменные добавлены, но Railway все еще не видит их

---

## ❌ Текущая ошибка

```
ValidationError: 3 validation errors for Settings
database_url - Field required
openai_api_key - Field required
secret_key - Field required
```

**Это означает, что переменные либо:**
1. Добавлены в неправильный сервис
2. Не применены к правильному окружению
3. Имеют неправильные имена
4. Деплой не перезапущен после добавления

---

## 🔍 Диагностика

### Проверка 1: Правильный ли сервис?

**ВАЖНО:** Переменные должны быть в сервисе **ПРИЛОЖЕНИЯ**, а не в PostgreSQL!

1. Railway Dashboard → Проект
2. Проверьте список сервисов:
   - **PostgreSQL** - база данных (❌ НЕ ЭТОТ!)
   - **AI-Automating-customer-messages** или другой - приложение (✅ ЭТОТ!)

3. Откройте сервис **ПРИЛОЖЕНИЯ** (не PostgreSQL!)
4. Variables → проверьте, что переменные там есть

### Проверка 2: Правильные ли имена?

Переменные должны называться **ТОЧНО** так:
- ✅ `DATABASE_URL` (заглавными буквами)
- ✅ `OPENAI_API_KEY` (заглавными буквами)
- ✅ `SECRET_KEY` (заглавными буквами)

**НЕ правильно:**
- ❌ `database_url` (строчными)
- ❌ `DATABASE_URL_` (с подчеркиванием в конце)
- ❌ `DATABASE-URL` (с дефисом)

### Проверка 3: Правильные ли значения?

**DATABASE_URL:**
- ✅ Должен начинаться с `postgresql+asyncpg://`
- ❌ НЕ `postgresql://` (без `+asyncpg`)

**OPENAI_API_KEY:**
- ✅ Должен быть реальным ключом (начинается с `sk-`)
- ❌ НЕ `sk-XXXXX` или примеры

**SECRET_KEY:**
- ✅ Минимум 32 символа
- ❌ НЕ меньше 32 символов

### Проверка 4: Применены ли к окружению?

В Railway переменные могут быть применены к разным окружениям. Убедитесь, что они применены к **Production**.

---

## ✅ РЕШЕНИЕ: Пошаговая проверка

### Шаг 1: Проверьте сервис

1. Railway Dashboard → Проект
2. Найдите сервис **ПРИЛОЖЕНИЯ** (не PostgreSQL!)
3. Откройте его
4. Variables → проверьте список переменных

### Шаг 2: Проверьте каждую переменную

Для каждой переменной проверьте:

**DATABASE_URL:**
- [ ] Имя точно `DATABASE_URL` (заглавными)
- [ ] Значение начинается с `postgresql+asyncpg://`
- [ ] Значение не пустое

**OPENAI_API_KEY:**
- [ ] Имя точно `OPENAI_API_KEY` (заглавными)
- [ ] Значение начинается с `sk-`
- [ ] Значение не пустое и не пример

**SECRET_KEY:**
- [ ] Имя точно `SECRET_KEY` (заглавными)
- [ ] Значение минимум 32 символа
- [ ] Значение не пустое

### Шаг 3: Перезапустите сервис

После проверки/изменения переменных:

1. Railway Dashboard → Сервис приложения
2. Settings → **Restart Service**
3. Или Deploy → **Redeploy**

### Шаг 4: Проверьте логи

После перезапуска проверьте Deploy Logs:
- Ошибка `ValidationError` должна исчезнуть
- Должны появиться логи успешного запуска

---

## 🎯 Быстрое решение

1. **Railway Dashboard** → Проект
2. **Сервис ПРИЛОЖЕНИЯ** (не PostgreSQL!)
3. **Variables** → проверьте список:
   - Должны быть видны все 3 переменные
   - Имена заглавными буквами
   - Значения правильные
4. Если переменных нет или они неправильные:
   - Удалите старые (если есть)
   - Добавьте новые с правильными именами и значениями
5. **Settings** → **Restart Service**
6. Подождите 1-2 минуты
7. Проверьте логи - ошибка должна исчезнуть

---

## ⚠️ ЧАСТЫЕ ОШИБКИ

### Ошибка 1: Переменные в PostgreSQL сервисе
**Симптом:** Переменные добавлены, но ошибка остается  
**Решение:** Удалите из PostgreSQL, добавьте в сервис ПРИЛОЖЕНИЯ

### Ошибка 2: Неправильные имена
**Симптом:** Переменные добавлены, но ошибка остается  
**Решение:** Используйте точно `DATABASE_URL`, `OPENAI_API_KEY`, `SECRET_KEY` (заглавными)

### Ошибка 3: Неправильный формат DATABASE_URL
**Симптом:** DATABASE_URL добавлен, но ошибка остается  
**Решение:** Измените `postgresql://` на `postgresql+asyncpg://`

### Ошибка 4: Сервис не перезапущен
**Симптом:** Переменные добавлены правильно, но ошибка остается  
**Решение:** Перезапустите сервис вручную: Settings → Restart Service

---

## 🔍 Как проверить, что переменные видны

### В Railway Dashboard:

1. Сервис ПРИЛОЖЕНИЯ → Variables
2. Должны быть видны:
   - ✅ `DATABASE_URL` = `postgresql+asyncpg://...`
   - ✅ `OPENAI_API_KEY` = `sk-...`
   - ✅ `SECRET_KEY` = `...` (минимум 32 символа)

### В логах Railway:

После перезапуска в Deploy Logs не должно быть ошибки `ValidationError`.

---

## ✅ После исправления

После правильного добавления переменных и перезапуска:

1. Railway автоматически перезапустит сервис
2. Deploy Logs → ошибка должна исчезнуть
3. Приложение должно успешно запуститься
4. Health endpoint должен работать

---

**ГЛАВНОЕ:** 
- Переменные должны быть в сервисе **ПРИЛОЖЕНИЯ**, а не PostgreSQL!
- Имена должны быть заглавными буквами!
- DATABASE_URL должен иметь формат `postgresql+asyncpg://`!
- После добавления перезапустите сервис!

🚀 **Время на исправление:** ~5 минут


# ✅ Чеклист для исправления Railway

**Дата:** 2025-11-27  
**Репозиторий:** https://github.com/Viphunter83/AI-Automating-customer-messages

---

## 🔧 Что нужно исправить

### 1. ✅ Обновить `requirements.txt` в корне

**Проблема:** Неполный список зависимостей  
**Решение:** ✅ Исправлено - добавлен `mangum`

---

### 2. ✅ Создать `runtime.txt`

**Проблема:** Railway может использовать неправильную версию Python  
**Решение:** ✅ Создан `runtime.txt` с `python-3.10.11`

---

### 3. ⚠️ **ГЛАВНАЯ ПРОБЛЕМА: Переменные окружения**

**Проблема:** Railway не может найти обязательные переменные  
**Решение:** Нужно добавить в Railway Dashboard:

#### Обязательные переменные:

1. **DATABASE_URL**
   - Получить из PostgreSQL сервиса в Railway
   - Изменить формат: `postgresql://` → `postgresql+asyncpg://`
   - Или использовать: `{{PostgreSQL.DATABASE_URL}}` и изменить формат

2. **OPENAI_API_KEY**
   - Значение: `sk-tsnn3jx66aPvVtcGGte3MxRJ35DT8Uyz`
   - Или ваш реальный ключ

3. **SECRET_KEY**
   - Значение: `bCTR2fSwMxOvisSFpKh0qnQKt2NY4jJRLzR_5R8MI5Q`
   - Или сгенерировать новый: `python3 -c "import secrets; print(secrets.token_urlsafe(32))"`

#### Опциональные переменные:

4. **OPENAI_API_BASE** = `https://api.proxyapi.ru/openai/v1`
5. **OPENAI_MODEL** = `gpt-4o-mini`
6. **DEBUG** = `False`
7. **LOG_LEVEL** = `INFO`

---

## 📋 Пошаговая инструкция

### Шаг 1: Закоммитьте исправления

```bash
git add requirements.txt runtime.txt
git commit -m "Fix Railway configuration: add mangum and runtime.txt"
git push
```

### Шаг 2: Добавьте переменные в Railway

1. Откройте [railway.app](https://railway.app)
2. Проект: **AI-Automating-customer-messages**
3. Сервис приложения → **Variables**
4. Добавьте 3 обязательные переменные (см. выше)

### Шаг 3: Проверьте деплой

1. Railway автоматически перезапустит сервис
2. Проверьте логи - ошибка должна исчезнуть
3. Проверьте health endpoint:
   ```bash
   curl https://your-project.railway.app/health
   ```

---

## ✅ Итоговый чеклист

- [x] `requirements.txt` обновлен (добавлен `mangum`)
- [x] `runtime.txt` создан
- [x] `railway.toml` настроен правильно
- [x] `Procfile` существует
- [ ] **Переменные окружения добавлены в Railway Dashboard** ⚠️
- [ ] Деплой успешен
- [ ] Health endpoint работает

---

## 🎯 Главная проблема

**Отсутствуют переменные окружения в Railway Dashboard!**

Это основная причина ошибки:
```
ValidationError: 3 validation errors for Settings
database_url - Field required
openai_api_key - Field required
secret_key - Field required
```

**Решение:** См. `RAILWAY_FIX_ENV.md` для подробных инструкций.

---

**После добавления переменных Railway должен успешно запустить приложение!** 🚀


# 🚂 Инструкция по деплою на Railway

**Дата:** 2025-11-26  
**Проблема:** Railway не может найти команду запуска

---

## ✅ Решение

Созданы два файла конфигурации:

1. **`railway.toml`** - основная конфигурация Railway
2. **`Procfile`** - резервная конфигурация

---

## 🚀 Что делать сейчас

### Шаг 1: Закоммитьте файлы в Git

```bash
git add railway.toml Procfile
git commit -m "Add Railway configuration for FastAPI deployment"
git push
```

### Шаг 2: Railway автоматически перезапустит деплой

После push Railway автоматически:
- Обнаружит новые файлы
- Применит конфигурацию из `railway.toml`
- Запустит приложение с правильной командой

---

## 📋 Альтернатива: Настройка вручную в Dashboard

Если автоматическая конфигурация не сработает:

1. Откройте ваш проект в [Railway Dashboard](https://railway.app)
2. Перейдите в **Settings** → **Service**
3. Найдите раздел **Start Command**
4. Вставьте команду:
```
cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT
```
5. Сохраните изменения
6. Railway перезапустит сервис

---

## 🔧 Что делает конфигурация

**railway.toml:**
```toml
[build]
builder = "NIXPACKS"

[deploy]
startCommand = "cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
```

**Procfile:**
```
web: cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT
```

Команда:
1. Переходит в папку `backend/`
2. Запускает `uvicorn` с приложением из `main.py`
3. Слушает на всех интерфейсах (`0.0.0.0`)
4. Использует порт из переменной `$PORT` (Railway автоматически устанавливает)

---

## ⚙️ Переменные окружения

Убедитесь, что в Railway настроены переменные:

**Обязательные:**
```
DATABASE_URL=postgresql+asyncpg://postgres:PASSWORD@HOST:PORT/railway
OPENAI_API_KEY=sk-your-key
SECRET_KEY=your-secret-key-minimum-32-characters
```

**Опциональные:**
```
OPENAI_API_BASE=https://api.proxyapi.ru/openai/v1
OPENAI_MODEL=gpt-4o-mini
DEBUG=False
LOG_LEVEL=INFO
```

---

## ✅ Проверка после деплоя

После успешного деплоя проверьте:

```bash
# Health check
curl https://your-project.railway.app/health

# API docs
open https://your-project.railway.app/docs

# Тест API
curl -X POST https://your-project.railway.app/api/messages/ \
  -H "Content-Type: application/json" \
  -d '{"client_id": "test", "content": "Привет"}'
```

---

## 💡 Рекомендация

**Для FastAPI лучше использовать Vercel:**
- ✅ Лучшая поддержка serverless
- ✅ Автоматическое масштабирование
- ✅ Edge Functions
- ✅ Бесплатный план с хорошими лимитами

**Railway лучше для:**
- ✅ PostgreSQL базы данных (рекомендуется!)
- ✅ Долгоживущие процессы
- ✅ WebSocket соединения

**Идеальная комбинация:**
- **Railway** → PostgreSQL база данных
- **Vercel** → FastAPI Backend (serverless)

---

## 🎯 Следующие шаги

1. ✅ Закоммитьте `railway.toml` и `Procfile`
2. ✅ Запушьте в GitHub
3. ✅ Railway автоматически перезапустит деплой
4. ✅ Проверьте логи в Railway Dashboard
5. ✅ Протестируйте API

---

**Готово!** После коммита Railway должен успешно запустить приложение! 🚀


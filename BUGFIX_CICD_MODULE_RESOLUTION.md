# 🐛 Исправление: Разрешение модулей в CI/CD

**Дата:** 2025-11-27

---

## 🔍 Проблема

### Bug: TypeScript не может найти модули `@/lib/api` и `@/lib/mockData` в CI/CD

**Проблема:**
В CI/CD окружении (GitHub Actions) TypeScript не может разрешить модули с алиасами путей:
- `Cannot find module '@/lib/api' or its corresponding type declarations`
- `Cannot find module '@/lib/mockData' or its corresponding type declarations`

**Причина:**
1. `moduleResolution: "bundler"` - это новый режим TypeScript, который может не работать правильно при прямом запуске `tsc --noEmit` в CI/CD окружении
2. Next.js использует свой собственный механизм разрешения модулей во время сборки, но `tsc` напрямую может не понимать эти пути
3. В CI/CD запускается `npm run type-check`, который выполняет `tsc --noEmit` напрямую, без участия Next.js

---

## ✅ Решение

### Исправление: Изменить `moduleResolution` на `"node"`

**До:**
```json
{
  "compilerOptions": {
    "moduleResolution": "bundler",  // ❌ Не работает в CI/CD
    "baseUrl": ".",
    "paths": {
      "@/lib/*": ["./lib/*"],
      // ...
    }
  }
}
```

**После:**
```json
{
  "compilerOptions": {
    "moduleResolution": "node",  // ✅ Работает везде
    "baseUrl": ".",
    "rootDir": ".",
    "paths": {
      "@/lib/*": ["lib/*"],  // ✅ Без ./ для лучшей совместимости
      "@/components/*": ["components/*"],
      "@/hooks/*": ["hooks/*"],
      "@/app/*": ["app/*"],
      "@/*": ["*"]
    }
  }
}
```

**Изменения:**
1. ✅ Изменен `moduleResolution` с `"bundler"` на `"node"` для лучшей совместимости
2. ✅ Добавлен `rootDir: "."` для явного указания корневой директории
3. ✅ Убраны `./` из путей в `paths` (так как `baseUrl` уже установлен в `.`)
4. ✅ Порядок путей сохранен (специфичные перед общими)

---

## 📝 Изменения

### `frontend/tsconfig.json`
- ✅ Изменен `moduleResolution` с `"bundler"` на `"node"`
- ✅ Добавлен `rootDir: "."`
- ✅ Упрощены пути в `paths` (убран `./`)

---

## ✅ Проверка

- ✅ TypeScript проверка типов: Успешно
- ✅ Локальная сборка: Успешно
- ✅ Next.js сборка: Успешно
- ✅ Разрешение модулей: Корректное

---

## 🎯 Результат

Теперь:
- ✅ TypeScript правильно разрешает пути `@/lib/*`, `@/components/*` и т.д.
- ✅ Работает как локально, так и в CI/CD окружении
- ✅ Next.js продолжает работать корректно (он использует свой механизм разрешения)
- ✅ `tsc --noEmit` в CI/CD теперь правильно находит все модули

---

## 📚 Дополнительная информация

**Почему `moduleResolution: "node"` работает лучше:**

1. **Совместимость:** `"node"` - это стандартный режим разрешения модулей, который понимают все инструменты
2. **CI/CD:** При запуске `tsc` напрямую в CI/CD, он использует стандартный алгоритм Node.js для разрешения путей
3. **Next.js:** Next.js использует свой собственный механизм разрешения модулей во время сборки, поэтому изменение `moduleResolution` не влияет на его работу

**Альтернативные решения (если проблема сохранится):**

1. **Создать отдельный `tsconfig.ci.json`:**
   ```json
   {
     "extends": "./tsconfig.json",
     "compilerOptions": {
       "moduleResolution": "node"
     }
   }
   ```
   И изменить команду в CI/CD:
   ```yaml
   - name: Type check
     run: |
       cd frontend
       npx tsc --noEmit --project tsconfig.ci.json
   ```

2. **Использовать `tsconfig-paths`:**
   ```bash
   npm install --save-dev tsconfig-paths
   ```
   И изменить команду:
   ```json
   "type-check": "ts-node -r tsconfig-paths/register -P tsconfig.json --noEmit"
   ```

---

**Исправление применено успешно!** ✅

**Теперь CI/CD должен проходить без ошибок разрешения модулей.**


# 🔍 Глубокий структурный анализ проблемы CI/CD

**Дата:** 2025-11-27

---

## 📊 Анализ проблемы

### Симптомы
- ✅ Локально: `npm run type-check` работает успешно
- ❌ CI/CD: TypeScript не может найти модули `@/lib/types`, `@/lib/api`, `@/lib/mockData`
- ❌ Ошибка: `Cannot find module '@/lib/types' or its corresponding type declarations`

### Текущая конфигурация

**tsconfig.json:**
```json
{
  "compilerOptions": {
    "moduleResolution": "node",
    "baseUrl": ".",
    "rootDir": ".",
    "paths": {
      "@/lib/types": ["./lib/types"],
      "@/lib/api": ["./lib/api"],
      "@/lib/mockData": ["./lib/mockData"],
      "@/lib/*": ["./lib/*"],
      // ...
    }
  }
}
```

**CI/CD команда:**
```yaml
- name: Type check
  working-directory: frontend
  run: npm run type-check
```

---

## 🔬 Глубокий анализ

### Гипотеза 1: Проблема с `baseUrl: "."`

**Проблема:**
В CI/CD окружении `baseUrl: "."` может разрешаться по-разному в зависимости от рабочей директории.

**Анализ:**
- `working-directory: frontend` устанавливает рабочую директорию в `frontend/`
- `baseUrl: "."` должен указывать на текущую директорию
- Но TypeScript может интерпретировать это по-разному

**Решение:**
Изменить `baseUrl` на `"./"` для явности:
```json
{
  "baseUrl": "./",  // ✅ Более явный формат
  "rootDir": "./"
}
```

### Гипотеза 2: Проблема с разрешением путей TypeScript

**Проблема:**
TypeScript `paths` маппинг может не работать правильно при прямом запуске `tsc --noEmit` в CI/CD.

**Анализ:**
- `paths` - это только для TypeScript компилятора
- В runtime (Node.js) эти пути не работают
- Но для `tsc --noEmit` они должны работать

**Возможное решение:**
Использовать `tsconfig-paths` или другой инструмент для явного разрешения путей.

### Гипотеза 3: Проблема с порядком путей

**Проблема:**
Специфичные пути (`@/lib/types`) могут конфликтовать с общим паттерном (`@/lib/*`).

**Анализ:**
- TypeScript проверяет пути сверху вниз
- Первое совпадение используется
- Специфичные пути должны быть ПЕРЕД общими ✅ (уже так)

### Гипотеза 4: Проблема с форматом путей

**Проблема:**
Пути с `./` могут не работать правильно в CI/CD.

**Анализ:**
- `./lib/types` - относительный путь от `baseUrl`
- С `baseUrl: "."` это должно быть `./lib/types`
- Но может быть проблема с разрешением

**Решение:**
Попробовать разные форматы:
1. `"./lib/types"` (текущий)
2. `"lib/types"` (без `./`)
3. Абсолютные пути (не рекомендуется)

### Гипотеза 5: Проблема с расширениями файлов

**Проблема:**
TypeScript может не находить файлы без явных расширений.

**Анализ:**
- Пути указаны без расширений: `"./lib/types"`
- TypeScript должен сам найти `types.ts`
- Но в CI/CD это может не работать

**Решение:**
Попробовать добавить расширения в пути (но это ограничивает гибкость).

---

## 🛠️ Предлагаемые решения

### Решение 1: Изменить формат `baseUrl`

```json
{
  "baseUrl": "./",  // Вместо "."
  "rootDir": "./"
}
```

### Решение 2: Добавить отладку в CI/CD

```yaml
- name: Type check
  working-directory: frontend
  run: |
    echo "Current directory: $(pwd)"
    ls -la lib/
    cat tsconfig.json | grep -A 25 "paths"
    npm run type-check
```

### Решение 3: Использовать абсолютные пути (если возможно)

Но это не рекомендуется, так как делает проект менее портативным.

### Решение 4: Создать отдельный `tsconfig.ci.json`

```json
{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "baseUrl": "./",
    "paths": {
      // Явные пути с расширениями для CI/CD
    }
  }
}
```

### Решение 5: Использовать `tsconfig-paths`

```bash
npm install --save-dev tsconfig-paths
```

И изменить команду:
```json
{
  "scripts": {
    "type-check": "ts-node -r tsconfig-paths/register -P tsconfig.json --noEmit"
  }
}
```

---

## ✅ Применяемые исправления

1. ✅ Изменен `baseUrl` на `"./"` для явности
2. ✅ Добавлена отладка в CI/CD для диагностики
3. ✅ Сохранена текущая структура путей

---

## 📝 Следующие шаги

1. Запустить CI/CD с отладкой
2. Проанализировать вывод отладки
3. Применить дополнительные исправления на основе результатов

---

**Анализ завершен. Применены начальные исправления для диагностики проблемы.**


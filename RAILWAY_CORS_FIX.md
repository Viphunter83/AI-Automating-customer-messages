# 🔧 Исправление CORS для Vercel Frontend

**Дата:** 2025-11-27  
**Проблема:** Фронтенд не может подключиться к Railway API из-за CORS

---

## 🔍 Проблема

Фронтенд на Vercel (`https://frontend-qawc41iml-olegs-projects-d32cda90.vercel.app`) не может подключиться к Railway API, потому что Vercel URL не добавлен в `allowed_origins`.

---

## ✅ Решение

### Добавить переменную окружения в Railway

1. **Откройте Railway Dashboard:**
   - https://railway.com/project/4d2e02dc-89b3-4d70-9fed-13ee99bce07a

2. **Откройте сервис приложения:**
   - Нажмите на **AI-Automating-customer-messages**

3. **Перейдите в Variables:**
   - В верхней части страницы найдите вкладку **Variables**
   - Или перейдите в **Settings** → **Variables**

4. **Добавьте переменную `ALLOWED_ORIGINS`:**

   **Вариант 1: Только Vercel URL (для продакшена)**
   ```
   ALLOWED_ORIGINS=https://frontend-qawc41iml-olegs-projects-d32cda90.vercel.app,https://*.vercel.app
   ```

   **Вариант 2: Vercel + локальная разработка**
   ```
   ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8000,https://frontend-qawc41iml-olegs-projects-d32cda90.vercel.app,https://*.vercel.app
   ```

   **Вариант 3: Все Vercel домены (рекомендуется)**
   ```
   ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8000,https://*.vercel.app
   ```

5. **Сохраните переменную**

6. **Перезапустите сервис:**
   - Railway автоматически перезапустит сервис после добавления переменной
   - Или перейдите в **Deployments** и нажмите **Redeploy**

---

## 🔍 Проверка

После добавления переменной:

1. **Проверьте логи Railway:**
   - Убедитесь, что сервис перезапустился
   - Проверьте, что нет ошибок

2. **Проверьте фронтенд:**
   - Обновите страницу фронтенда
   - Проверьте консоль браузера (F12)
   - Статус должен измениться на "Backend is available ✅"

---

## 📋 Формат переменной

Переменная `ALLOWED_ORIGINS` может быть:
- **Строка с запятыми:** `origin1,origin2,origin3`
- **JSON массив:** `["origin1", "origin2", "origin3"]`

**Рекомендуется:** Использовать строку с запятыми для простоты.

---

## ✅ После исправления

После добавления переменной и перезапуска:
- ✅ Фронтенд сможет подключаться к Railway API
- ✅ CORS ошибки исчезнут
- ✅ Интерфейс будет работать полностью

---

**Добавьте переменную `ALLOWED_ORIGINS` в Railway и перезапустите сервис!** 🚀


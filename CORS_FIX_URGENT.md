# 🔴 СРОЧНО: Исправление CORS для подключения фронтенда

**Проблема:** Фронтенд на Vercel не может подключиться к Railway backend  
**Ошибка:** "No response from server. Please check your connection."

---

## ✅ Решение: Добавить Vercel URL в ALLOWED_ORIGINS

### Шаг 1: Откройте Railway Dashboard

1. Перейдите на: https://railway.com
2. Откройте проект: **AI-Automating-customer-messages**
3. Откройте сервис: **AI-Automating-customer-messages** (backend service)

### Шаг 2: Добавьте переменную ALLOWED_ORIGINS

1. **Перейдите в раздел Variables:**
   - В верхней части страницы найдите вкладку **Variables**
   - Или: **Settings** → **Variables**

2. **Добавьте новую переменную:**
   - Нажмите **+ New Variable** или **+ Add Variable**
   - **Key:** `ALLOWED_ORIGINS`
   - **Value:** 
     ```
     https://frontend-zeta-taupe-21.vercel.app,https://*.vercel.app,http://localhost:3000,http://localhost:8000
     ```
   - Нажмите **Add** или **Save**

### Шаг 3: Перезапустите сервис

Railway автоматически перезапустит сервис после добавления переменной.  
Или вручную: **Deployments** → **Redeploy**

---

## 🔍 Проверка

После перезапуска (1-2 минуты):

1. **Обновите страницу фронтенда:**
   - https://frontend-zeta-taupe-21.vercel.app

2. **Попробуйте отправить сообщение:**
   - Перейдите в `/demo`
   - Отправьте тестовое сообщение
   - Ошибка должна исчезнуть

3. **Проверьте консоль браузера (F12):**
   - Не должно быть CORS ошибок
   - Запросы должны проходить успешно

---

## 📋 Формат переменной

**Рекомендуемое значение:**
```
https://frontend-zeta-taupe-21.vercel.app,https://*.vercel.app,http://localhost:3000,http://localhost:8000
```

**Объяснение:**
- `https://frontend-zeta-taupe-21.vercel.app` - основной production URL
- `https://*.vercel.app` - все Vercel preview URLs (для будущих деплойментов)
- `http://localhost:3000` - локальная разработка фронтенда
- `http://localhost:8000` - локальная разработка backend

---

## ⚠️ Важно

- Переменная должна быть добавлена в **backend service**, а не в database service
- После добавления переменной Railway автоматически перезапустит сервис
- Подождите 1-2 минуты после перезапуска перед проверкой

---

## 🎯 После исправления

✅ Фронтенд сможет подключаться к Railway API  
✅ CORS ошибки исчезнут  
✅ Сообщения будут отправляться успешно  
✅ Интерфейс будет работать полностью

---

**Добавьте переменную `ALLOWED_ORIGINS` в Railway прямо сейчас!** 🚀

